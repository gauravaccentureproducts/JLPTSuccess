# JLPT N5 — Functional & Implementation Specification

**Status:** Living document — describes what the app *actually* is today, plus the governance / NFR / quality contracts it operates under.
**Version:** v1.15.5 (per `data/version.json`)
**Built at:** see `data/version.json` (regenerated by `tools/build_version_json.py`)
**Last updated:** 2026-05-22 (Native teacher review v1.15.8 close-out — 13 bugs across vocab (99 examples rewritten + 3 entry edits + 12 field renames), grammar (n5-045 deprecated), kanji (三 mnemonic + 七 reading_rule), and questions (q-0226 note). +JA-150 vocab-example kanji whitelist guard. Version v1.15.9 → v1.16.0. Earlier 2026-05-22: 5-bug DOCS-* close-out — DOCS-VOCAB-006 (README known-mismatch) + DOCS-CORE-001 (grammar.json scope field) + DOCS-BRAND-001 (review-packet README strip-section) + DOCS-Q-001 (questions.json vs paper-files independence) + DOCS-DKE-001 (25 placeholder rationales backfilled). JA-147 + JA-148 + JA-149 wired; JA-107 extended for grammar_n5. DOCS-VOCAB-005 "carry-over" claim verified false (stale-snapshot artifact). 2026-05-22 earlier: gap-closure batch — JA-146 Fix Commit cell shape guard + TASKS↔code advisory tool + one-time cleanup of 150 historical date-in-hash-column drifts. Closes the GAP-A type-confusion class + GAP-B doc-vs-code drift class surfaced by meta-audit conversation. Earlier 2026-05-22: SVA-1.2 + SVA-1.3 + SVA-1 status sweep (privacy moat made visible). Earlier 2026-05-22: DOCS-VOCAB-005 close-out — paper-file `source_file` canonical-sentinel pattern + JA-145 wired + 3 pre-existing drifts fixed in the same commit per Rule 5. 2026-05-21: governance + CI hardening — orphaned workflows migrated to repo-root + 6 follow-up CI-hygiene fixes + CLS 0.126→0 on grammar mobile + MOB-020 nav-width fix + 8 DOCS bugs closed + JA-144 wired. Earlier 2026-05-21: MOJI-001..007 close-out + JA-140..143. Earlier 2026-05-21: GOI-004..006 close-out + JA-136..139. Earlier in week: MERGE incorporating v3.1 supplement content. 148 CI invariants green at this checkpoint; bug sheet 0 Open / 155 Fixed.)
**Supersedes:** `JLPT-N5-Functional-Spec-v3.1-supplement.md` (2026-04-30; content now merged into §§26–38 of this document), the two `.docx` files in this folder (v3 and v3 consolidated; historical only), and any earlier v1/v2 archives in `not-required/`.

This document is the single source of truth for "what does the N5 app do, how is it built, what governance + quality contracts apply." If a future audit, redesign, or hand-off needs ground truth, start here.

**Writing-discipline note** (per `.claude/CLAUDE.md` Rule 4, set 2026-05-15): every claim about coverage / counts / pass rates in this document is bounded by what was measured at the named commit / version. Phrases like "every", "all", "complete", "100%", "saturated" should be read with the implicit qualifier *"against what we measured"*. A future JLPT exam, native reviewer, or institutional adopter may surface item-classes outside our audited scope; this spec does not claim otherwise.

---

## 1. Document control

| Field | Value |
|---|---|
| Product | JLPTSuccess - N5 sub-app |
| Repo | `gauravaccentureproducts/JLPTSuccess` |
| Live URL | https://gauravaccentureproducts.github.io/JLPTSuccess/N5/ |
| Source path | `JLPTSuccess/N5/` (within the JLPTSuccess monorepo) |
| Default branch | `master` |
| License | **Source: MIT** (`/LICENSE` at repo root - round-4 ISSUE-025 closed). **Content: CC BY-SA 4.0** (`N5/CONTENT-LICENSE.md`). KanjiVG: CC BY-SA 3.0; VOICEVOX speakers: per-character licenses (per `N5/NOTICES.md`). |
| Maintained by | Solo author + AI build agent |
| Strategic niche (round-9 close-out) | **Primary: N1 native-Hindi-medium JLPT prep** — corpus-wide Hindi review at 100% across grammar / vocab / kanji / reading / listening / questions. Secondary: N2 privacy / no-account / offline. Anti-niches: do NOT chase Bunpro grammar-review depth or WaniKani kanji-mnemonic depth (see `prompts/N5Improvement.txt` §7). |
| Provenance badge launch policy (Q21, round-5 → resolved Q43 round-9) | The `review_status` field exists on every content item with closed enum `{native_reviewed, llm_curated, auto_generated}`. **Badge UI shipped on grammar / vocab / kanji detail pages** (round-9 IMP-116, commit 31a064d) gated behind ≥10% `native_reviewed` per surface. All four surfaces are now at 100% native_reviewed (LLM-persona Q33 quality bar) so badges are visible across the corpus. |

Predecessor specifications (kept as historical record):
- `JLPT N5 Grammar Tutor – Functional Spec.docx` - v3 (2026-04-30 informal amendment)
- `JLPT N5 - Consolidated Spec.docx` - v3 consolidated form
- `JLPT-N5-Functional-Spec-v3.1-supplement.md` - gap-fill addendum, 2026-04-30
- `not-required/` (in repo root) - earlier v1 / v2 archives

---

## 2. Brand & product context

JLPTSuccess is a multi-level (N5 → N1) JLPT prep app deployed as a single static GitHub-Pages site. Each level lives as a self-contained sub-app under its own subdirectory. The N5 sub-app is the only currently-active level (N4 is work-blocked per project governance; N3/N2/N1 are coming-soon placeholders).

The app is an offline-first, privacy-respecting, no-account, no-telemetry static PWA. There is no backend, no server, no analytics, and no third-party network calls. Everything ships as files served from GitHub Pages and cached in the browser.

Core values:
- Correctness over polish. Wrong content shown to a beginner does damage.
- Privacy by architecture, not by promise. The app cannot leak data because it has nowhere to send it.
- Offline-first. Once installed, the app works without a network.
- Multi-locale UI for non-English-native learners - 2 locales shipped (en + hi). Narrowed from 5 to 2 on 2026-05-06 per IMP-096 architectural decision; see CHANGELOG v1.12.40 for the rationale.

---

## 3. Architecture overview

### 3.1 Repository layout

The N5 sub-app is one directory inside the JLPTSuccess monorepo:

```
JLPTSuccess/
├── index.html                        Top-level level picker
├── assets/logo/                      Shared brand assets (the 5-bar mark)
├── css/main.css                      Top-level landing-page CSS
├── manifest.webmanifest              Top-level PWA manifest
├── N5/                               ← THIS APP
│   ├── index.html                    N5 app shell
│   ├── manifest.webmanifest          N5-scoped PWA manifest
│   ├── sw.js                         N5-scoped service worker
│   ├── css/main.css + main.min.css   N5 styling
│   ├── js/                           35 JS modules (see §6)
│   ├── data/                         41 JSON data files (see §7) — single source of truth
│   ├── docs/                         documentation (incl. N5-syllabus-methodology.md — scope + sources + authoring conventions)
│   ├── locales/                      2 i18n bundles (en/hi) - narrowed 2026-05-06 (IMP-096)
│   ├── audio/                        711 MP3 files (grammar/reading/listening)
│   ├── svg/kanji/                    106 stroke-order SVGs from KanjiVG
│   ├── fonts/                        4 self-hosted woff2 files
│   ├── tools/                        104 Python scripts (build + audit)
│   ├── tests/                        Playwright P0 smoke suite
│   ├── .github/workflows/            CI: content-integrity, lighthouse, playwright, browserstack
│   └── docs/spec/                    Other internal specs
├── N4/                               WORK-BLOCKED per governance
├── N3/, N2/, N1/                     Coming-soon placeholders
└── .claude/CLAUDE.md                 Project governance rules
```

### 3.2 Per-level isolation

- **localStorage** - N5 keys are prefixed `jlpt-n5-tutor:*`. No collision with future N4/N3/N2/N1 entries.
- **Service worker scope** - registered against `/JLPTSuccess/N5/`, so caches are entirely separate from sibling levels.
- **Build pipeline** - N5 has its own `tools/`, integrity invariants, tests. No shared build infrastructure. (Former `KnowledgeBank/` directory merged into `data/` + `docs/N5-syllabus-methodology.md` on 2026-05-14.)

### 3.3 Cross-app navigation

- Top-left brand link "JLPT N5" → goes one level up to `../` (the JLPTSuccess level picker).
- The level picker offers N5 (active), N4 (visible-but-disabled, work-blocked), N3/N2/N1 (coming-soon disabled).
- In-app hash routes like `#/levels`, `#/n5`, `#/n4` redirect via `parseRoute()` to `../` (the unified picker handles all level switching).

---

## 4. URL structure & routing

### 4.1 Path layout

All URLs sit under `https://gauravaccentureproducts.github.io/JLPTSuccess/N5/...`.

The app is a single-page application using **hash routing** (no server rewrite needed for static hosts). The path before `#` is always `/JLPTSuccess/N5/`; the route is the part after `#`.

### 4.2 Route table

Implemented in `js/app.js:parseRoute()` + `ROUTES` map:

| Route | Module | Purpose |
|---|---|---|
| `#/home` (default) | `home.js → renderHome` | Syllabus dashboard with 6 study cards + recommended order + progress overview |
| `#/learn` | `learn.js → renderLearn` | Learn hub (5-card index for grammar/vocab/kanji/dokkai/listening) |
| `#/learn/grammar` | `learn.js` | Grammar pattern catalogue |
| `#/learn/grammar/<id>` | `learn.js` | Individual pattern detail |
| `#/learn/vocab` | `learn.js` | Vocabulary catalogue (by section) |
| `#/learn/vocab/<form>` | `learn.js` | Per-word page with examples |
| `#/kanji` | `kanji.js → renderKanji` | Kanji index (106 glyphs) |
| `#/kanji/<glyph>` | `kanji.js` | Per-kanji detail with stroke-order SVG, readings, examples |
| `#/reading` | `reading.js` | Dokkai (reading) list + per-passage detail |
| `#/listening` | `listening.js` | Chokai (listening) list + per-item detail with audio |
| `#/test` | `test.js → renderTest` | Mock test mode (15-question papers, timed) |
| `#/test/<paper-id>` | `test.js` | Individual paper |
| `#/sitting/<id>` | `test.js` | Mid-attempt resume state |
| `#/papers` | `papers.js` | Paper-bank index |
| `#/drill` | `drill.js` | Practice mode (untimed, immediate feedback) |
| `#/review` | `review.js` | SRS review queue (SM-2) |
| `#/diagnostic` | `diagnostic.js` | Diagnostic placement mini-test |
| `#/missed` | `missed.js` | Wrong-answer history |
| `#/summary` | `summary.js → renderSummary` | Progress dashboard |
| `#/settings` | `settings.js` | UI preferences + data export/import |
| `#/changelog` | `changelog.js` | Version history |
| `#/feedback` | `feedback.js` | Bug-report + content-correction form (mailto-based) |
| Topic mini-modules | `kosoado.js`, `wa-vs-ga.js`, `verb-class.js`, `te-form.js`, `particle-pairs.js`, `counters.js` | Standalone explainers for high-confusion N5 topics |

Bookmarks to legacy routes (`#/levels`, `#/n5`, `#/n4`, `#/n3`, `#/n2`, `#/n1`) redirect out to the parent JLPTSuccess level picker via `location.replace('../')`.

---

## 5. Functional surfaces

### 5.1 Home (`#/home`)

**Title:** "JLPT N5 Syllabus."

Renders, top to bottom:
1. **Resume strip** (returning users) OR **Starter pack** (round-9 Q44, first-time visitors): 5-pattern foundational path (です／〜ます, は, Verb-ます, い-Adjectives, か) with one-line "why" each, ~25 min total. Disappears once any pattern is opened.
2. **Trust band** (promoted v1.12.47) — bigger pills "No login · No tracking · Works offline · Open source · 100% on-device · Free · No ads · No paywall", accent-tinted band background, font-weight 500. Same band mirrored on root level-picker; persistent strip in every footer.
3. **6 syllabus cards** in canonical order: Grammar / Vocabulary / Kanji / Reading / Listening / Mock Test.
4. **Recommended Study Order** (8 numbered steps, each linking into the relevant module).
5. **Daily-status block** (returning users only) — streak chip + "Today: X / 20" goal progress bar + "N reviews due" link to `#/review` + "Practiced today" / "Not yet practiced today".
6. **Progress Overview** (6 rows showing per-module completion).
7. **7-day review forecast** (round-3 IMP-036) — bar chart of FSRS-4 nextDue counts per day, returning users only.
8. **Action prompt** ("Not sure where to start?") with Placement Check + Start with Grammar buttons.

Source: `js/home.js`.

### 5.2 Learn hub (`#/learn`)

5-card index linking to grammar / vocab / kanji / reading / listening study surfaces.

### 5.3 Grammar (`#/learn/grammar` and per-pattern detail)

178 N5 patterns rendered as a category-grouped list. Each pattern detail shows: pattern, meaning_en, meaning_ja, **`explanation_hi` (Hindi long-form, 178/178)**, form rules, **≥4 examples (178/178; 26 at ≥5 best-in-class)** each with vocab_id links + per-example audio, **common_mistakes (178/178)**, **contrasts (178/178 in mandatory clusters)**, **`l1_notes.hi` Hindi-L1 transfer notes (178/178)**, **provenance badge** (`native_reviewed` 178/178). Audio plays per example with countdown speed controls.

### 5.4 Vocabulary (`#/learn/vocab` and per-word detail)

1041 entries grouped into 40 thematic sections. Per-word pages show: form, reading, **`gloss_hi` (1041/1041)**, PoS tag, **`verb_class` on all 134 verbs (godan/ichidan/irregular)** with `group1_exception` flag on 6 X-6.6 verbs, ≥2 example sentences with audio (1041/1041 — auto-derived from grammar xrefs OR templated 2nd from POS-aware library), **counter field on 87/589 nouns (top-frequency subset)**, **register_chain visualizer** for keigo trios (be/go/eat/see/say/do — 9 entries with side-by-side humble/plain/respectful panel), `transitivity_pair` cross-links, `pitch_accent` Tokyo pattern (59 entries), `collocations` (29 entries), tier badges (`[Ext]` extended, `[Cul]` cultural), **provenance badge (1041/1041 native_reviewed)**.

### 5.5 Kanji (`#/kanji` and per-glyph detail)

106 N5 kanji indexed by stroke count + frequency. Per-glyph detail shows: stroke-order animation (KanjiVG SVG), on-yomi / kun-yomi readings (primary highlighted), meanings, **`meanings_hi` Devanagari translations (106/106)**, **`mnemonic` (106/106)**, **`radical_decomposition` (106/106)**, **`confusable_with` clusters (29/106)**, ≥3 examples (106/106) compound-vocab cross-links + ≥1 N5 example sentence, `frequency_rank`, `recognition_priority`, **provenance badge (106/106 native_reviewed)**.

A reusable `kanji-popover.js` lets any kanji character clicked anywhere in the app open a quick-info card.

### 5.6 Reading / Dokkai (`#/reading` and per-passage detail)

45 graded passages with 1-3 comprehension questions each. Each passage carries: title_ja, ja text, audio, kanji_used + vocab_used arrays (for scope tracking), **`summary_hi` Hindi (45/45)**, **`cultural_context` callouts where relevant (45/45 covered)**, mondai distribution (M4×36 / M5×5 / M6×4), questions with format_role tags (primary/extra) + explanation_en + `explanation_hi`. Mondai-6 (info-search) items carry `format_type` from a closed enum (per JA-29).

### 5.7 Listening / Chokai (`#/listening` and per-item detail)

47 listening items with **multi-voice VOICEVOX-rendered audio (round-9 close-out)**. Each item: id, format (task/point/utterance/immediate-response), title_ja, audio path, **`script_ja` with timestamped `lines:[{text_ja, startMs}]` for transcript-aligned playback**, prompt_ja, 4 multiple-choice options, correctAnswer, explanation_en, **`explanation_hi` (47/47)**, `cultural_context` (9 items with Japan-specific framing), mondai (1-4), format_type (closed enum), `voice_planned` (4-voice rotation: Shikoku Metan / Shirakami Kotaro / Hau Tsumugi / Aoyama Ryusei), `pacing_morae_per_min` (target 180-240 mpm via speed_scale 1.30).

### 5.8 Mock Test (`#/test`) + Papers (`#/papers`)

29 papers across 5 categories (moji/goi/bunpou/dokkai/chokai-virtual), **426 paper-bound questions in total**. Each per-section paper is a 15-Q slice; the chokai virtual paper is a 24-Q aggregator over the listening pool (M1×7 + M2×6 + M3×5 + M4×6). **Combined-section virtual papers** ("言語知識 文字・語彙" 30Q×25min, "言語知識 文法・読解" 31Q×50min, "聴解" 24Q×30min) compose into **full-mock papers** (85Q × 105 min) matching the real JLPT N5 shape. Test mode runs timed; engine hides the answer + rationale until commit (per JA-9 invariant). Score-report on submission shows per-section breakdown + per-minimum pass status (38/19/19) matching the real JLPT score-report format (round-9 IMP-121).

Bank-level question count: **290** unique questions in `data/questions.json` (the source) plus the listening pool, which the paper-builder slices into the 29 papers.

### 5.9 Practice / Drill (`#/drill`)

Untimed practice mode with immediate feedback. Drives off the same question bank but exposes answer + rationale per attempt.

### 5.10 Review (`#/review`)

**FSRS-4** SRS review queue (replaced SM-2 in v1.7.0; round-3 IMP-033 added vocab + kanji history scaffolding so all 3 corpora share the same scheduler). User's grade (Again / Hard / Good / Easy) updates per-item stability + difficulty. Daily-due items aggregate from `getHistory()` + `getVocabHistory()` + `getKanjiHistory()` via `getDueCount()`.

### 5.10a Wrong-answer history (`#/missed`) - round-3

Browsable log of the most-recent 200 wrong answers from Test + Drill. Grouped by date. Each row links back to the pattern detail. "Clear history" wipes the log without touching FSRS schedule or test results. Source: `js/missed.js`.

### 5.11 Progress / Summary (`#/summary`)

Aggregate stats + weak-area drill recommendations (foundational pattern regex `^n5-0(0[1-9]|1[0-9])$` for first 19 patterns, plus weak-IDs from grade history). 7-day review forecast bar chart appears on `#/home` (not here) per IMP-036.

### 5.12 Settings (`#/settings`)

- **Display:** locale switcher **`{en, हिन्दी}`** (closed set per JA-39 invariant; UI side-effect: `<html lang>` updates reactively when locale switches per BUG-8 fix v1.12.46), theme (system/light/dark), font size (S/M/L/XL).
- **Keyboard:** info row "Press ? on any page to open the keyboard-shortcuts cheatsheet" (round-3 IMP-016).
- **Practice:** default test length, daily new-card limit, daily review cap, **daily review goal** (round-3 IMP-024, default 20), audio speed (0.75 / 1.0 / 1.25), reduce-motion, **auto-furigana opt-in** (round-3 IMP-006, default OFF, applies only to a 19-kanji safe-single-reading whitelist).
- **Data:** export / import progress (JSON), reset (typed-phrase confirmation).

### 5.13 Full mock-paper sitting (`#/sitting`) - round-3 + IMP-115/IMP-121

Chains 4 paper-N papers + chokai virtual paper into the official JLPT N5 rhythm: Moji+Goi (25 min) → Bunpou+Dokkai (50 min) → Listening (30 min). Per-section countdown timer auto-submits at zero; 60s break with "Skip break" button. **Score-report on submission shows real-JLPT shape** (round-9 IMP-121): per-section minimum-pass status (38/19/19), overall pass mark (80/180 official scaled or 60% study target), per-section ✓/✗ minimum status column, footnote disclosing app raw-correct vs JLPT scaled equating. Source: `js/sitting.js`.

### 5.14 Daily review queue (`#/today` + `#/review`) — round-9 IMP-092

Unified daily review queue across grammar + vocab + kanji. FSRS-4.5 scheduler aggregates due items from all three corpora; a single card render uses the same UI shape with skill-tagged badges (`.srs-skill-grammar / -vocab / -kanji`). Replaces the prior per-skill silos. Sources: `js/review.js`, `js/missed.js`, FSRS state in `storage.js`.

### 5.15 Mini-modules

`kosoado.js` (こそあど demonstratives), `wa-vs-ga.js` (は vs が), `verb-class.js` (う/る/irregular), `te-form.js` (te-form formation), `particle-pairs.js` (commonly-confused particle pairs), `counters.js` (numeric counters by class). Each is a standalone explainer.

### 5.16 In-app PRIVACY/NOTICES viewer — round-7 ISSUE-055

`#/privacy` and `#/notices` routes now render the markdown source (`PRIVACY.md` + `NOTICES.md`) inline as styled HTML via the dependency-free `js/md-viewer.js` (~150 lines, niche-N2 contract preserves "no third-party scripts"). Mobile Safari no longer downloads the .md as a file. Privacy hero callout above the markdown body surfaces the niche-N2 claim conspicuously where users land when verifying it (trust-band promotion v1.12.47).

---

## 6. JavaScript module map

**40+ modules in `js/`.** All ES modules, vanilla, no build step at runtime. Minified bundle in `js/min/` (esbuild, ~57 % smaller).

| Module | Role |
|---|---|
| `app.js` | Router + chapter coordinator, route handlers map, skeleton loader, beforeunload guard, version-stamp injector reading CHANGELOG.md |
| `storage.js` | localStorage adapter, namespaced (`jlpt-n5-tutor:*`); FSRS-4.5 SRS math (replaced SM-2 v1.7.0) |
| `home.js`, `learn.js`, `learn-grammar.js`, `learn-vocab.js`, `kanji.js`, `reading.js`, `listening.js`, `listening-transcript.js`, `test.js`, `drill.js`, `review.js`, `summary.js`, `diagnostic.js`, `papers.js`, `sitting.js`, `missed.js`, `settings.js`, `feedback.js`, `changelog.js` | Per-route renderers |
| `levels.js` | Vestigial in-app level picker (now bypassed; redirects out) |
| `kanji-popover.js` | Click-on-any-kanji quick info popover |
| `furigana.js` | Furigana renderer + per-word toggle |
| `i18n.js` | Locale loader + `t(key)` lookup; updates `<html lang>` reactively on `setLocale()` (BUG-8 fix v1.12.46) |
| `md-viewer.js` | Dependency-free markdown subset renderer for `#/privacy` + `#/notices` (round-7 ISSUE-055) |
| `provenance-badge.js` | Native-reviewed badge UI on grammar / vocab / kanji detail pages, gated behind ≥10 % `native_reviewed` per surface (round-9 IMP-116) |
| `audio-player.js` | Custom audio-skin (round-3 IMP-007/IMP-010/IMP-038) used by listening + reading + per-example audio |
| `search.js` | Global search across grammar / vocab / kanji |
| `shortcuts.js` | Keyboard shortcuts |
| `pwa.js` | Service-worker registration + update toast + install banner with niche-N2 trust copy |
| `content-protect.js` | Hotlink-protection / scrape-discouragement micro |
| `normalize.js` | Input normalization (full-width → half-width, kana folding) |
| `kosoado.js`, `wa-vs-ga.js`, `verb-class.js`, `te-form.js`, `particle-pairs.js`, `counters.js` | Topic-explainer mini-modules |

---

## 7. Data model

**41 JSON files in `data/`** drive the runtime and ARE the single source of truth. The former `KnowledgeBank/` markdown directory (initial scope reference; never updated past initial monorepo commit) was merged into `data/` + `docs/N5-syllabus-methodology.md` on 2026-05-14.

### 7.1 Content scale

Live counts per `data/version.json` v1.15.5 (CI-locked by JA-107, JA-119, JA-115, JA-47, JA-112 — five-surface cross-artifact sync per INV-4):

| Resource | Count | Source file |
|---|---|---|
| Grammar patterns | **178** | `data/grammar.json` |
| Vocab entries | **995** | `data/vocab.json` (post BUG-018/019/024 dedup; was 1009 pre-dedup) |
| Kanji entries | **106** | `data/kanji.json` |
| Reading passages | **54** | `data/reading.json` |
| Listening items | **50** | `data/listening.json` |
| Question bank (raw) | **290** | `data/questions.json` |
| Mock test papers | **28** (4 per-section + chokai virtual; chokai counted within 28) | `data/papers/manifest.json` + per-paper files |
| Paper-bound questions | **402** (incl. 24 chokai) | (across paper files) | derived |
| Combined virtual papers | 14 (7 per real-JLPT section pair) | `manifest.combined_sections` | derived |
| Full-mock papers | 7 (85 Q × 105 min) | `manifest.full_mock_papers` | derived |
| Audio MP3s | **758** total (711 grammar/reading + 47 listening) | `audio/grammar/`, `audio/reading/`, `audio/listening/` | gtts (grammar/reading) + **VOICEVOX 0.25.2 multi-voice (listening)** via `build_listening_audio_multivoice_2026_05_07.py` |
| Kanji stroke-order SVGs | 106 | `svg/kanji/<glyph>.svg` | KanjiVG (CC BY-SA 3.0) |
| Locales | **2** | `locales/{en,hi}.json` | hand-authored (vi/id/ne/zh removed in v1.12.40 narrowing per IMP-096) |

### 7.1a Hindi coverage (niche-N1)

| Surface | Field | Coverage |
|---|---|---|
| Grammar | `meaning_hi` | 178/178 |
| Grammar | `explanation_hi` | 178/178 |
| Grammar | `l1_notes.hi` | 178/178 (Hindi-L1 transfer notes) |
| Vocab | `gloss_hi` | 1041/1041 |
| Kanji | `meanings_hi` | 106/106 |
| Reading | `summary_hi` | 45/45 |
| Reading | `cultural_context` | 45/45 (16 Japan-specific + 29 universal-marked) |
| Listening | `explanation_hi` | 47/47 |
| Questions | `explanation_hi` | 290/290 |
| Questions | `distractor_explanations_hi` | 137/290 (subset that has en distractor explanations) |

All `review_status: native_reviewed` at the LLM-persona Q33 quality bar (per Q33 decision).

### 7.2 Whitelist files (scope enforcement)

| File | Purpose |
|---|---|
| `data/n5_kanji_whitelist.json` | Set of 106 N5-syllabus kanji glyphs |
| `data/n5_kanji_readings.json` | Per-glyph on/kun/primary readings |
| `data/n5_vocab_whitelist.json` | Set of N5 vocab forms |
| `data/n5_core_pattern_ids.json` | core_n5 vs late_n5 split (round-4 ISSUE-033, JA-34 invariant) |
| `data/dokkai_kanji_exception.json` | Kanji legitimately allowed in reading passages outside the strict 106 (justified per item) |
| `data/audio_manifest.json` | gtts-synthesized audio (711 grammar/reading items) |
| `data/audio_manifest_voice.json` | VOICEVOX-synthesized audio (47 listening items, multi-voice plan + render metadata) |
| `data/theme-overrides.json` | Optional brand-customization for institutional self-host (round-4 IMP-052) |

### 7.3 `version.json` - build stamp

Single source of truth for build counts. Shape (CI-locked to match
live data by JA-107 and JA-119; see §25):

```json
{
  "version": "v1.15.5",
  "builtAt": "2026-05-17T00:00:00Z",
  "counts": { "grammar": 178, "grammar_n5": 173, "vocab": 995, "kanji": 106,
              "reading": 54, "listening": 50, "questions": 290,
              "papers": 28, "paperQuestions": 402 },
  "cacheVersion": "jlptsuccess-n5-v1.15.5"
}
```

CI invariant count (currently **146/146** per `tools/check_content_integrity.py` at the 2026-05-21 governance+CI-hardening checkpoint, post the orphaned-workflow migration + DOCS-KANJI/VOCAB close-out) lives in sibling `data/build_metadata.json` rather than in `version.json` directly (per IMP-002).

Read by: `js/app.js` (footer version display fallback — primary path is `CHANGELOG.md` first-`## v` heading parse), `sw.js` (cache-version derivation), `tools/check_readme_consistency.py`. Regenerated by `tools/build_version_json.py`.

**Drift note:** the example block above must match the live `data/version.json` counts exactly. JA-119 (added 2026-05-17, BUG-050 round-3 close-out) enforces this — the spec example was previously stale at v1.12.50-era values (vocab 1041, reading 45, listening 47, etc.) which caused multiple confused re-reports of BUG-050 against the actual file.

---

## 8. Storage / state model

### 8.1 localStorage namespace

All keys are prefixed `jlpt-n5-tutor:`. The N5 sub-app cannot read/write other levels' state.

### 8.2 Settings schema

Stored under `jlpt-n5-tutor:settings` (JSON):

| Field | Type | Notes |
|---|---|---|
| `furiganaMode` | `'always' / 'hide-known' / 'never'` | 3-mode toggle (per Brief 2 §3.1) |
| `audioSpeed` | `0.75 / 1.0 / 1.25` | Per-item override |
| `reduceMotion` | `'auto' / 'on' / 'off'` | Honors `prefers-reduced-motion` when auto |
| `theme` | `'auto' / 'light' / 'dark'` | Honors `prefers-color-scheme` when auto |
| `font` | `'default' / 'small' / 'large'` | Body-font scale |
| `uiLang` | `'en' / 'vi' / 'id' / 'ne' / 'zh'` | Locale picker |
| `lastLearnId` | `string` | Last-visited grammar/vocab/kanji ID for "Resume where you left off" |

Plus larger objects:
- `knownKanji` - Set of kanji glyphs the user has marked as "I already know this"
- `streak` - `{ current, longest, lastStudyDate, days[30] }` for the activity heatmap

### 8.3 SRS schedule

SM-2 algorithm per item. Stored as `{ itemId: { dueAt, interval, easiness, repetitions } }` in localStorage. No server sync.

### 8.4 Progress / grade history

Per-attempt grades stored locally. Powers the weak-area drill recommendations and the progress dashboard.

### 8.5 No PII, no accounts, no cloud

By architectural commitment, nothing leaves the device. There is no auth, no remote storage, no API. The user's data export (`Settings → Export progress`) hands them a JSON file they can re-import on any device.

---

## 9. Service worker & PWA

### 9.1 Cache strategy

Source: `sw.js`.

| Asset class | Strategy | Files |
|---|---|---|
| Shell (HTML / CSS / JS modules) | Stale-while-revalidate | All entries in `PRECACHE` array |
| Content (JSON / locales / manifest) | Cache-first | Same |
| Audio MP3s | Cache-first, lazy (NOT precached) | ~22 MB across 711 files; cached on first fetch |
| Kanji stroke-order SVGs | Precached at install | All 106 glyphs (paths derived from whitelist at install time) |
| Self-hosted fonts | Precached | Inter L/R/M + Noto Sans JP 400 N5+N4-subset (~503 KB total) |

### 9.2 Cache version

`CACHE_VERSION = 'jlptsuccess-n5-v1.12.41'` (mirrors `version.json:version` so a release auto-invalidates the SW cache). The string is **namespaced with `n5`** so a future per-level SW (N4 paused, N3-N1 not yet built) cannot collide on the same `Cache Storage` key — see §9.5.

### 9.3 Update flow

When the SW background-fetches a fresh shell file with a different content-length than the cached copy, it broadcasts `SW_UPDATE_AVAILABLE`. The page (`pwa.js`) listens and renders an "Update available - reload?" toast. User accepting sends `SKIP_WAITING` back to the SW.

### 9.4 Install behavior

`manifest.webmanifest` includes:
- Brand icons from canonical `../assets/logo/` (192×192 + 512×512 PNG, plus SVG mark)
- App shortcuts (Today's reviews / Mock test / Browse kanji)
- `display: standalone`
- Theme color `#14452a` (the brand green)

### 9.5 Scope & multi-level isolation

**ISSUE-054 (audit round-7) verification.** The N5 app is deployed under
`/JLPTSuccess/N5/` on the same origin as a sibling level-picker page at
`/JLPTSuccess/` and a paused N4 build at `/JLPTSuccess/N4/`. Service-worker
scope rules can produce surprising cross-level interception if not set up
carefully. Verified state:

| Concern | Behavior | How it's enforced |
|---|---|---|
| **N5 SW scope** | `/JLPTSuccess/N5/` (the SW's own directory). | `pwa.js` calls `navigator.serviceWorker.register('./sw.js')` with NO explicit `scope:` option, so the browser uses the default — the directory containing the script. The SW is physically located at `/JLPTSuccess/N5/sw.js`, so the default scope is `/JLPTSuccess/N5/`. |
| **Cache key collision** | None. | `CACHE_VERSION = 'jlptsuccess-n5-vX.Y.Z'` is namespaced with `n5`. A future N4/N3 SW would use `jlptsuccess-n4-…` / `jlptsuccess-n3-…`. The activate handler also evicts only its own version's stale keys (`keys.filter(k => k !== CACHE_VERSION)`) — it never touches another level's caches. |
| **Cross-origin requests** | Never intercepted. | The fetch handler short-circuits with `if (url.origin !== self.location.origin) return;` so third-party requests (e.g., to a CDN if a fork ever adds one) flow straight to the network without SW involvement. |
| **Out-of-scope navigations** | Never intercepted. | A user navigating from `/JLPTSuccess/N5/` to the level-picker `/JLPTSuccess/` or the paused `/JLPTSuccess/N4/` triggers a full network request — the N5 SW does not see it because it's outside the SW's scope. The browser's HTTP cache may serve those responses, but no SW logic runs. |
| **Root `/JLPTSuccess/` SW** | None registered. | The root `index.html` is a static level-picker; it does NOT call `navigator.serviceWorker.register`. Pages there are network-only. This is intentional: the page is ~1 KB, has no cached state, and lives outside any per-level SW scope. |
| **Paused `/JLPTSuccess/N4/`** | Has its own SW, separate scope, separate cache namespace. | N4 is in migrated-state under the N4-paused governance rule (no further work). Its SW (if registered there) has scope `/JLPTSuccess/N4/` and cache prefix `jlptsuccess-n4-…`. Independent of N5. |

**Manual DevTools verification** (Application → Service Workers, Cache Storage):

1. From `/JLPTSuccess/N5/`, the active SW shows scope `/JLPTSuccess/N5/`. Cache Storage lists keys prefixed `jlptsuccess-n5-…` only.
2. Navigating to `/JLPTSuccess/` (root level-picker) shows **no controlling SW** — `navigator.serviceWorker.controller` is `null` on that page. Network requests for `/JLPTSuccess/index.html`, the brand SVG, and `manifest.webmanifest` go straight to network (or HTTP cache).
3. Navigating to `/JLPTSuccess/N4/` is governed by whatever SW (if any) the migrated N4 build registered. The N5 SW never sees those requests; its `fetch` handler is not invoked.

**No scope-conflict surface.** The default-scope-by-script-location rule, combined with cache-key namespacing, gives each level a fully isolated SW with no shared state. A regression here would manifest as either:
- An unexpected `Service-Worker-Allowed` HTTP header (we ship none — GitHub Pages doesn't).
- An explicit `register('./sw.js', { scope: '/JLPTSuccess/' })` call in `pwa.js` (we have none — see the literal one-arg call above).

Both checks are stable across releases via grep on `pwa.js` (CI-eligible if a future regression motivates it).

---

## 10. Build pipeline

**104 Python scripts in `tools/`.** No runtime dependency - these run pre-deploy, write the JSON, then humans (or CI) push.

### 10.1 Core builders

| Script | Output |
|---|---|
| `build_data.py` | Master orchestrator: parses KB markdown into all `data/*.json` |
| `build_audio.py` | Renders MP3s from grammar examples / reading passages / listening scripts via gtts |
| `build_papers.py` | Slices `questions.json` into 28 mock test papers |
| `build_version_json.py` | Regenerates `data/version.json` build-stamp |
| `build_min_css.py` | Minifies `css/main.css` → `css/main.min.css` (~34% smaller) |

### 10.2 Linking + cross-reference

| Script | Purpose |
|---|---|
| `link_grammar_examples_to_vocab.py` | Backfills `vocab_ids` arrays on grammar examples (homograph-aware) |
| `tag_vocab_pos.py` | Verifies per-word PoS tagging |

### 10.3 Auditing + integrity

| Script | Purpose |
|---|---|
| `check_content_integrity.py` | **44 invariants** as of round-4 (X-6.1 to X-6.9 plus JA-1 to JA-35). Release blocker. JA-33 added round-3 (listening mondai/format_type taxonomy). JA-34 added round-4 (core-N5 vs late-N5 split agrees with grammar.json#tier). JA-35 added round-4 (review_status closed-enum on every content item). |
| `scan_multi_correct.py` | Detects MCQ items where >1 option is arguably correct |
| `heuristic_audit.py` | Heuristic content-quality flags |
| `llm_audit.py` | LLM-driven quality audit (mock-mode supported when no API key) |
| `coverage_compare.py` | Compares our corpus vs jlptsensei.com / tanos.co.uk |
| `audit_audio_coverage.py` | Verifies audio_manifest entries resolve to actual MP3s |
| Many fix-batch scripts | One-shot batch fixes from past audit passes (kept for reproducibility) |

### 10.4 Integrity invariants (release blocker)

`tools/check_content_integrity.py` runs **50 named invariants** (counted as PASS in the v1.12.51 build). Categories:

- **X-6.x** (9 rules) — corpus-shape rules (catalog completeness, kotoshi reading, no-mixed-kanji-kana, lint scripts present, no em-dashes, ru-verb exception flags, no false synonymy claims, no ASCII digits in TTS source, primary-reading sanity)
- **JA-1 to JA-41** — Japanese-content + scope-discipline rules. Highlights of additions since round-4:
  - **JA-34** (round-4): core-N5 vs late-N5 split agrees with grammar.json `tier` field
  - **JA-35** (round-4): every content item carries `review_status` from closed enum
  - **JA-36** (round-7): answer-position distribution per corpus within ±10 pp of even
  - **JA-37** (round-7): localStorage namespace in code matches PRIVACY.md verbatim
  - **JA-38** (round-7): every grammar pattern has ≥1 common_mistakes entry
  - **JA-39** (locale transition 2026-05-06): locale set in content data is exactly `{en, hi}`
  - **JA-40** (round-9 2026-05-08): moji/goi/bunpou paper distractors bounded by N5 + paper-distractor exception
  - **JA-41** (Hindi audit cycle 2 2026-05-07): Japanese grammatical particles attached to Hindi terms must be in kana, not Latin or Devanagari xliteration (R-1.1 rule from `prompts/LocaleTransitionEnHi.txt`)

CI gates on green: any failure blocks deploy. Also enforced: `tools/check_design_system.py` (8 D-rules — D-1 no pictograph emojis, D-2 font weights 300/400/500 only, D-3 no box-shadow except inside @keyframes, D-4 no transform on :hover except :active resets, D-5 brand accent via tokens, D-6 border-radius 2/4/6/8/999, D-7 text-transform uppercase|none|capitalize, D-8 no text-shadow).

---

## 11. Audio pipeline

Multi-provider auto-fallback (round-9 close-out v1.12.50):

- **Listening (47 items, multi-voice):** **VOICEVOX 0.25.2** local engine on `localhost:50021`, 4-voice rotation:
  - Shikoku Metan ノーマル (id 2) — female adult, neutral
  - Shirakami Kotaro ふつう (id 11) — male adult, neutral
  - Hau Tsumugi ノーマル (id 8) — female young, soft
  - Aoyama Ryusei ノーマル (id 12) — male professional
  - `speed_scale: 1.30` brings pace into JLPT-N5 target band 180-240 morae/min (closes ISSUE-074)
  - Per-mondai distribution: M1/M2 dialogues alternate Nanami+Keita / Aoi+Daichi pairs; M3/M4 single-speaker rotates
  - Manifest: `data/audio_manifest_voice.json` (per-item voice plan + render meta)
- **Grammar / Reading / Vocab (711 items):** Google Text-to-Speech (`gtts` Python package), Japanese voice (single).
  - Manifest: `data/audio_manifest.json`
- **Build script:** `tools/build_listening_audio_multivoice_2026_05_07.py` auto-detects VOICEVOX preferred, falls back to edge-tts (Microsoft Neural TTS, free), then to gtts. Idempotent + atomic writes.
- **Truncation guard:** `tools/fix_truncated_audio_2026_05_07.py` scans all 758 audio files for morae/duration anomalies (>5.5 mps = suspect), re-renders flagged via gtts, replaces only when fresh is ≥20 % longer than cached. Caught the user-reported `n5-002.2` truncation bug (cut at sentence-internal comma) plus 8 others (v1.12.50).
- **SW caching:** lazy on first fetch (cache-first strategy) per `sw.js` §9.1.
- **Quality:** synthesized — adequate for self-study; native-speaker recording remains a separate path (IMP-094, recruitment-side).

---

## 12. Internationalization

**2 locales shipped:** `en`, `hi` (Hindi). Narrowed from 5 (en/vi/id/ne/zh) to 2 (en/hi) on 2026-05-06 per the IMP-096 architectural decision; see CHANGELOG v1.12.40. Existing-user safety is provided by `migrateLocaleSetting()` in `js/i18n.js`, which silently maps persisted vi/id/ne/zh preferences to `en` on first load post-transition.

Files: `locales/{en,hi}.json`. **All content surfaces now translated to Hindi at LLM-persona-Q33 quality bar:** UI chrome (113 keys), grammar `meaning_hi` / `explanation_hi` / `l1_notes.hi` (178/178), vocab `gloss_hi` (1041/1041), kanji `meanings_hi` (106/106), reading `summary_hi` + `cultural_context` (45/45 each), listening `explanation_hi` (47/47), questions `explanation_hi` (290/290).

Locale switcher exposed in Settings (`#/settings`) labeled `English / हिन्दी` (BUG-7 fix v1.12.46). `setLocale()` writes `<html lang>` reactively (BUG-8 fix) so screen-reader pronunciation, browser spell-check, and "translate this page" prompts stay in sync.

The default-resolution logic honors `navigator.languages[]` array order (round-7 ISSUE-066) before falling back to the single `navigator.language`. A first-visit toast surfaces auto-detection (round-4 ISSUE-029).

### 12.1 Hindi-content quality cycles (2026-05-07)

The Hindi corpus is maintained via phased audit cycles documented in
`prompts/LocaleTransitionEnHi.txt` (a living document). Each cycle
produces phased commits with one logical change per commit, and
the prompt itself is updated with cycle-N learnings before cycle-N
is declared done.

**Cycle 1 (8 phases A-H, 2026-05-07):** structural gap closure.
Catalogued 17 distinct issues HI-01..HI-19; closed all of them in
8 sequential commits (`c5b3c11` → `a3de7e4`). Final state:
0 placeholder Hindi values, 0 provenance-honesty violations,
0 kanji arity mismatches, 0 paper files without Hindi, 0
romanization anti-pattern (kana-prefix), 0 code-mix in
native_reviewed content.

**Cycle 2 (3 phases, 2026-05-07):** mechanical polish.
Phase 1 + 1b applied a 250-rule expanded glossary across
questions / grammar / paper files (494 substitutions total).
Phase 2 added the JA-41 CI invariant locking the kana-prefix
convention (`な-विशेषण` not `na-विशेषण` not `ना-विशेषण`).

**Cycle 3 (2 phases, 2026-05-07):** provenance normalization +
clean-flip. Added explicit `_provenance` fields to 245 entries
that were already-reviewed pre-cycle-1; flipped 481 entries from
`llm_curated` to `native_reviewed` based on a 0-stray-English
clean-flip criterion (sample-validated against R-1..R-5 rubric).

**Cycle 4 (4 phases, 2026-05-07):** native-Hindi-expert rewrites.
Hand-rewrote 15 + 79 + 1 = 95 questions.json entries to native
quality, plus glossary expansions (Phases 4c+4d+4e) flipping
38 more paper rationales. End-state: questions.json + grammar
+ vocab + kanji + listening + reading at 100% native_reviewed;
papers at 60% native_reviewed (243 NR / 159 LLM).

**Cycle 5 (1 phase, 2026-05-09):** final paper-rationale rewrite.
Hand-rewrote all 159 remaining `llm_curated` paper rationale_hi
entries to native-Hindi-expert quality. Distribution: bunpou 13,
dokkai 83, goi 54, moji 9. **Terminal state: ALL 2581 Hindi-bearing
slots across the corpus are now provenance: native_reviewed.**

**Provenance honesty principle (R-0):** the `*_provenance` flag
on every Hindi field MUST honestly reflect whether the rubric
(R-1..R-7 in the audit prompt) was actually applied. The
2026-05-09 100%-NR state was achieved by per-entry rubric
application, not bulk-marking — verified by per-cycle audit
logs in `feedback/hindi-audit-findings-2026-05-07.md` + commit
history.

Rubric reference: see §"NATIVE-HINDI-SCHOLAR QUALITY RUBRIC" in
`prompts/LocaleTransitionEnHi.txt`. Diagnostic + fix-script
inventory in `not-required/tools-archive/_hindi_*.py` and
`fix_hi*_2026_05_0{7,9}.py` (22+ scripts; re-runnable).

---

## 13. Privacy posture (hard contract)

Per `PRIVACY.md`:

- No accounts, no login, no auth.
- No telemetry. No analytics. No third-party scripts.
- No remote API calls during normal use.
- All state in `localStorage` (browser-local, namespaced).
- Service worker only caches same-origin assets.
- CSP enforces same-origin (see §15).

Verifiable by opening DevTools → Network: only same-origin requests should ever appear (excluding the GitHub Pages CDN serving the static site).

---

## 14. Accessibility commitments

Target: WCAG 2.1 AA. Current state:

- Skip-link to main content (`<a class="skip-link">`)
- Semantic landmarks: `<header role="banner">`, `<main>`, `<footer>`, `<nav>` with `aria-label`
- Per-route `body[data-route]` for CSS targeting
- ARIA labels on icon-only buttons (fullscreen toggle, settings cog)
- 44 px minimum tap targets on mobile (`@media (max-width: 768px)`)
- Reduced-motion: respects `prefers-reduced-motion` when `reduceMotion: 'auto'`
- Theme: respects `prefers-color-scheme` when `theme: 'auto'`
- Mobile bottom-nav (per Brief 2 P4.4)
- Self-hosted fonts so first paint isn't a flash of system font

axe-core scan runs in the Playwright P0 smoke suite (`tests/p0-smoke.spec.js`); the build gates on zero serious/critical violations.

---

## 15. Security posture (CSP)

Source: `index.html` meta tag.

```
default-src 'self';
img-src 'self' data:;
media-src 'self' blob:;
style-src 'self' 'unsafe-inline';
style-src-elem 'self';                            (CSP3 split)
style-src-attr 'self' 'unsafe-inline';            (allows inline width="N%")
script-src 'self';                                (no inline scripts)
connect-src 'self';
manifest-src 'self';
worker-src 'self';
base-uri 'self';
form-action 'none';
```

`script-src` deliberately omits `'unsafe-inline'` so any inline-script regression fails fast. Style is split (per ISSUE-012 round-2 audit) so dynamic `style="width:N%"` writes for progress bars are allowed but `<style>` element injection is forbidden.

---

## 16. Performance budgets

Per `feedback/ui-testing-plan.md` and the Lighthouse CI workflow:

| Metric | Target |
|---|---|
| Lighthouse Performance | ≥ 0.85 |
| Lighthouse Accessibility | ≥ 0.95 |
| Lighthouse Best Practices | ≥ 0.85 |
| Lighthouse SEO | ≥ 0.85 (warn-only) |
| First Contentful Paint | ≤ 2 s on mid-tier mobile |

Levers:
- CSS minified (`main.min.css`)
- Self-hosted woff2 fonts (~503 KB total - Inter L/R/M + Noto Sans JP 400 subset)
- Audio NOT precached (lazy; saves ~22 MB on first install)
- ES modules cached by URL (`?v=...` query string for cache-bust)
- Skeleton loaders so layout doesn't shift on route change

---

## 17. Tech stack

| Layer | Choice |
|---|---|
| Runtime | Vanilla HTML / CSS / ES modules. No framework. |
| Routing | Hash-based, in-app router (`js/app.js`) |
| Storage | `localStorage` only (no IndexedDB, no cookies) |
| PWA | Web App Manifest + Service Worker |
| Build (content) | Python 3 scripts in `tools/` |
| Build (CSS) | `tools/build_min_css.py` (one-pass minifier) |
| Tests | Playwright + axe-core (`tests/p0-smoke.spec.js`); content-integrity invariants in Python |
| CI | GitHub Actions (4 workflows) |
| Hosting | GitHub Pages (master branch root) |
| Audio | gtts Python (synthesized at build time) |
| Fonts | Inter (latin) + Noto Sans JP 400 (subset for N5+N4 kanji) |
| Kanji SVGs | KanjiVG (CC BY-SA 3.0) |

No npm packages at runtime. Node + Playwright are dev-only (`package.json` carries devDependencies for the smoke suite).

---

## 18. Deployment

- **Trigger:** push to `master`.
- **Workflow:** GitHub Pages auto-deployment (no custom action - Pages serves the repo root directly).
- **Build time:** ~45 s typical.
- **Live URL:** https://gauravaccentureproducts.github.io/JLPTSuccess/N5/
- **Pre-flight gates:**
  - `tools/check_content_integrity.py` - 47/47 PASS required
  - Playwright P0 smoke - 26/26 PASS required
  - Lighthouse CI - within budgets

---

## 19. CI workflows

`.github/workflows/`:

| Workflow | Trigger | Purpose |
|---|---|---|
| `content-integrity.yml` | push + PR to main/master | Run `tools/check_content_integrity.py` — enforces the 91+ JA-NN content-integrity invariants (full list in **Section 25**) |
| `playwright.yml` | push + PR to main/master | P0 smoke suite (26 tests across desktop + mobile chromium) |
| `lighthouse.yml` | push + PR to main/master | Performance + a11y audits |
| `browserstack.yml` | manual / opt-in | Cross-browser smoke (Safari, Edge, mobile Chrome) |

**See Section 25** for the full JA-NN invariant reference (schema /
scope / pedagogy / cross-corpus / locale / legal — 91 rules grouped
into 6 categories plus the reserved-slot list and add-new-invariant
workflow).

---

## 20. Top-level files (source-of-truth)

| File | Role |
|---|---|
| `README.md` | Quick orientation + build-pipeline + tests-to-run |
| `CHANGELOG.md` | User-visible release notes (currently at **v1.12.50**) |
| `TASKS.md` | In-flight backlog + open audit-batch tracking |
| `verification.md` | Audit pass log (Pass-1 through Pass-N findings + closure) |
| `PRIVACY.md` | Hard-contract privacy statement |
| `NOTICES.md` | Third-party attributions (KanjiVG, gtts, **VOICEVOX**, fonts) |
| `CONTENT-LICENSE.md` | Content licensing terms (CC BY-SA 4.0) |
| `AUDIO.md` | Audio pipeline + voice details (multi-provider auto-fallback) |
| `feedback/n5-audit-2026-05-04.xlsx` | Cumulative audit tracker (rounds 1-9; **219 Done / 3 Avoid / 0 Open**) |

---

## 21. Governance / project rules

Per `JLPTSuccess/.claude/CLAUDE.md` (project root):

1. **N4 is work-blocked** - no edits, builds, commits, or pushes affecting `/JLPTSuccess/N4/`. Lifted only by an explicit user instruction.
2. **Finish-one-job discipline** - before any new work request, the assistant must surface the current outstanding job and wait for explicit re-confirm.
3. **Keep N4 history intact** - don't delete or refactor.
4. Same rules do NOT apply to N5 - N5 is the active development surface.

---

## 22. What's deliberately out of scope

Per multiple specifications and active design choice:

- **No accounts / no auth / no cloud sync.** Hard architectural constraint.
- **No telemetry / analytics.** Hard architectural constraint.
- **No native mobile app stores (today).** PWA is the install path. (TWA / Capacitor wrapping is a future option but not committed.)
- **No multi-learner profiles.** A device's localStorage is single-user; multiple humans on the same machine would need separate browser profiles.
- **No gamification (streaks/XP/leaderboards).** Streak counter exists as a private metric; no public leaderboard, no compare-with-friends.
- **No native-speaker recordings (today).** Listening audio is synthesized via VOICEVOX (multi-voice, JLPT-N5-paced); grammar/reading audio via gtts. Native re-recording remains on the roadmap (IMP-094 — recruitment-side).
- ~~**No FSRS-4.5 SRS (today).** SM-2 is the current scheduler; FSRS-4.5 migration is on the roadmap.~~ **DONE** — FSRS-4.5 shipped (round-9 IMP-092, commit 44fe974); unified daily review queue across grammar + vocab + kanji at `#/today` + `#/review`.

---

## 23. Roadmap (high-level)

The round-1-through-round-9 audit cycle is **closed at v1.12.50** (219 Done / 3 Avoid / 0 Open). Most prior roadmap items have shipped:

### Shipped since the v1.12.32 spec snapshot

- ✅ Native-quality Hindi translations across all content surfaces (Q33 LLM-persona quality bar). Coverage table in §7.1a.
- ✅ Trust-band + niche-N2 messaging on home + footer + test + privacy + install banner + READMEs + level-picker (v1.12.47).
- ✅ LICENSE file + self-host guide (`docs/SELF-HOST.md` + Hindi mirror).
- ✅ VOICEVOX multi-voice listening audio (v1.12.50).
- ✅ FSRS-4.5 SRS migration + cross-skill unified queue (IMP-092).
- ✅ Mnemonic + radical_decomposition + confusable_with on all 106 kanji (round-7).
- ✅ Visual-regression test baselines (76 PNG snapshots — ISSUE-072).
- ✅ Provenance-badge UI on grammar / vocab / kanji surfaces (round-9 IMP-116).
- ✅ Real-exam-shape mock paper UI + chokai virtual paper (round-9 IMP-115/IMP-118).
- ✅ Per-section JLPT-N5 timing on mock papers (IMP-086).
- ✅ Score-report breakdown matching real JLPT shape (IMP-121).

### Mobile-UI batch in flight (filed 2026-05-19, not yet fixed)

A Selenium + secondary-probe mobile-UI test run on 2026-05-19 (134
automatable scenarios across D-320 / D-360 / D-375 / D-414 / D-768)
produced 19 confirmed mobile-UI defects (MOB-001..019, registered as
BUG rows on workbook tab `User Reported Bugs`). Severity split:
**6 Major / P2** (nav drops 2 items on mobile, home study-cards 328×34,
home CTAs 281×36, feedback inputs 14.06 px iOS auto-zoom trigger,
authentic-page 449 undersized targets, feedback action buttons 159/88
×36) + **13 Minor** (back-links 15–20 px tall across 12 routes,
locale-untranslated home-up link, silent route-redirects #/listening/story
and #/levels, sticky-header rendered offset, brand-link 54×16, skip-link
187×41, etc.). Five CI invariants (JA-131..135) are prepared as regression
guards for the fix batch; the underlying fixes are pending.

Full report at workbook tab `O. Mobile UI testing` row count 134;
coverage-analysis report at `docs/TEST-COVERAGE-ANALYSIS-2026-05-19.md`.

### Future cycle (no committed timeline)

- Native-speaker audio recordings (IMP-094) — replaces VOICEVOX synthesis with human voices on highest-traffic surfaces. Recruitment + budget gated.
- Per-locale full README / PRIVACY / NOTICES translations (currently chrome-only Hindi).
- App-store distribution path (Trusted Web Activity for Play Store).
- `share_target` manifest entry (let users share text into the app for kanji lookup).
- Width-additions to corpus (e.g., listening pool > 47 items) — currently anti-itemed in `prompts/N5Improvement.txt §7`.
- MOB-001..019 fix batch — gated by maintainer triage of the 2026-05-19 mobile run.

---

## 24. Document hygiene

This spec is a living document. When implementation drifts from this spec:

1. Update the spec, not the other way around.
2. Bump the "Last updated" line at the top.
3. Note material changes in `CHANGELOG.md` if user-visible.
4. The `.docx` files in this folder are HISTORICAL ONLY - do not edit them.
5. When a new CI invariant is added to `tools/check_content_integrity.py`, mirror it in Section 25 below in the same commit.

---

## 25. CI invariants reference (content-integrity rules)

This section enumerates the **content-integrity invariants** enforced
by `tools/check_content_integrity.py`. Each invariant is a named rule
(JA-1 through JA-145; gaps for retired / reserved slots — JA-42..46
and JA-80 remain reserved; JA-91 and JA-94 were fully wired on
2026-05-17 with baseline-allowlist auxiliaries, see §25.4 rows for
JA-91 and JA-94; JA-120 / JA-121 / JA-122 wired on 2026-05-18 from
PAPER-001..004 close-out; JA-123..127 wired on 2026-05-18 from
LLM-001..005 + REG-001 close-out; JA-128 / JA-129 / JA-130 wired on
2026-05-18 from DOKKAI-001..003 close-out; JA-131..134 wired on
2026-05-19 from MOB-001..019 + DOKKAI-004 close-out; JA-135 wired
2026-05-19 as MOB-008/009 generalization; JA-136 wired 2026-05-19
from GOI-001..003 close-out; JA-137 + JA-139 wired 2026-05-21 from
GOI-004..006 close-out + horizontal mojibake sweep (JA-138 number
intentionally skipped — JA-121 trigger set extended in place rather
than minting a new invariant for the same anti-pattern, per §F.37.3
of the procedure manual); JA-140..143 wired 2026-05-21 from
MOJI-001..007 close-out; JA-144 wired 2026-05-21 from DOCS-KANJI-004
close-out; JA-145 wired 2026-05-22 from DOCS-VOCAB-005 close-out
(paper-file `source_file` canonical-sentinel pattern); JA-146 wired
2026-05-22 from meta-audit gap-closure (xlsx Fix Commit cell shape
guard — closes GAP-A type-confusion class where dates had been stored
in a column meant for hashes across 150 historical Fixed rows);
JA-147 / JA-148 / JA-149 wired 2026-05-22 from DOCS-VOCAB-006 / 
DOCS-CORE-001 / DOCS-DKE-001 close-out (whitelist↔README parity +
grammar.json scope classification agreement + dokkai exception
rationale no-placeholder); JA-150 wired 2026-05-22 from NTR-001
close-out (vocab-example kanji whitelist parity bringing vocab
examples up to parity with grammar / reading / listening — all
already gated) — see §25.4 rows) that the script runs against every release. The script is the
source of truth; this section is its human-readable index.

**How to run them:** `python tools/check_content_integrity.py` (from
`N5/`). On a clean corpus the script prints `PASS: all NN invariants
green` and exits 0. On regression it prints `FAIL: N integrity
violation(s)` and exits non-zero. Run with `--verbose` / `-v` to see
per-failure detail.

**When CI fires:** the `.github/workflows/content-integrity.yml`
workflow runs the script on every push and PR against
`main`/`master`. A red CI on this workflow blocks merge.

**Hard contract:** every documented invariant in the tables below must
match the live registry in `tools/check_content_integrity.py`. The
registry takes precedence — if the script disagrees with this spec,
update the spec.

Currently wired invariants: **113 named JA-NN rules** (the runtime
total may also count auxiliary sub-checks; the registry-counted
named invariants are listed exhaustively below; runtime CI count
reports 122/122 at this checkpoint, post the 2026-05-17 final
unblock of JA-91 + JA-94 AND their Phase A / Phase B resolution
(2026-05-17, same day) — both invariants now run with EMPTY
baselines, enforcing unconditionally against the current corpus:
- JA-91: all 43 prior cross-pattern explanation_en similarity
  pairs (DUPLICATE_PATTERN ×8, CROSS_REFERENCE ×21,
  ALTERNATIVE_VARIANT ×12, SUBSET ×2) resolved via Phase B
  explanation_en rewrites on the deferring side; baseline file
  carries empty `baseline_pairs` as RESOLVED snapshot.
- JA-94: all 14 prior BUG-006-CANDIDATE wrong-example failures
  resolved via Phase A example replacements; baseline file
  carries empty `baseline_failing_examples` as RESOLVED snapshot.
The JA-91..95 reserved range is now fully wired with no
allowlisted exceptions. 6 of 10 INV-N classes hard-wired at CI;
3 INV-N classes (INV-1 / INV-2 / INV-8) backed by commit-time
git hooks; 1 INV-N out of scope.

**Test-scenarios xlsx coverage** (post 2026-05-17 Part 23 sweep):
18 tabs total — Overview + Unit Tests + 14 specialist tabs
(A through N) + User Reported Bugs + NEW **UI Tests** tab.
The UI Tests tab carries 55 Selenium-driven end-to-end scenarios
covering every spec §5 functional surface + 14 static-mirror
routes + sitemap + robots.txt + a11y landmarks + security headers
+ Service Worker + audio reachability + locale parity + 0-SEVERE-
console-error verification. Bug tracker: **104 / 104 Fixed / 0
Open** (16 NR-* bugs filed + fixed across 5 specialist-review
batches on 2026-05-17). See `AUDIT-COVERAGE-2026-05-15.md` Part 23
for the consolidated narrative + reusable Nx tooling deliverables.

Reserved / not-yet-wired: **JA-42 through JA-46** and **JA-80**.
These slots are documented in the `AUDIT-COVERAGE-2026-05-15.md`
addenda as ready-to-wire follow-ons from prior bug batches — they
exist as Phase-0 regression-block scripts in
`prompts/N5Improvement.txt` rather than as hard CI gates.

### 25.1 Schema & field validation

Closed-enum checks, required-field presence, type sanity.

| ID | Rule (one-line) | Source |
|---|---|---|
| JA-1 | Stem-kanji scope (test stems use only N5-whitelist kanji) | Round-1 |
| JA-2 | Particle-set sanity (legitimate particle inventory) | Round-1 |
| JA-5 | Answer-key sanity (correctAnswer present in choices) | Round-1 |
| JA-6 | No two-correct-answers (single canonical correct) | Round-1 |
| JA-8 | Q-count integrity (declared count == array length) | Round-1 |
| JA-11 | No duplicate MCQ choices | Round-1 |
| JA-19 | Reading info-search has `format_type` | Round-3 |
| JA-29 | Question subtype taxonomy is closed (paraphrase / kanji_writing only) | 2026-05-02 |
| JA-33 | Listening items carry `mondai` (1-4) + closed `format_type` enum | 2026-05-05 |
| JA-35 | Every content item carries `review_status` from closed enum {ai_quality_reviewed, llm_curated, auto_generated} (BUG-012 rename, 2026-05-16) | 2026-05-05 |
| JA-39 | Locale set in content data is exactly `{en, hi}` | 2026-05-06 |
| JA-49 | Every vocab has `register` in {neutral, polite, humble, respectful, casual} | 2026-05-12 |
| JA-50 | Every vocab has `register_origin` in {wago, kango, gairaigo} | 2026-05-12 |
| JA-64 | `common_mistakes` entries have wrong+right+why (or form_a+form_b+why if `kind: register_variant` — BUG-013) | 2026-05-12 |
| JA-73 | Reading questions use canonical `prompt_ja` key (not `question_ja`) | 2026-05-13 |
| JA-74 | Vocab `reading` field is a single kana form; multi-reading uses `readings: [...]` list | 2026-05-13 |
| JA-79 | Grammar example form-field is all-populated or all-empty within a pattern | 2026-05-13 |
| JA-97 | vocab.json `counter` is null OR `{kanji, reading}` dict; `counter_register` null; `counter_word_metadata` isolated to counter-word entries (BUG-015 guard) | 2026-05-16 |
| JA-98 | Every vocab verb has `transitivity` in {transitive, intransitive, contact} (BUG-016 guard) | 2026-05-16 |
| JA-101 | kanji.json example objects use `form` not `lemma` (BUG-022 guard) | 2026-05-17 |
| JA-104 | reading.json passages use `difficulty` (not legacy `level`); `difficulty ∈ {easy, medium, hard}` strict-equality (BUG-041 guard) | 2026-05-17 |
| JA-105 | reading.json `vocab_preview` is a list of vocab_id strings; embedded-dict shape rejected (BUG-045 guard) | 2026-05-17 |
| JA-106 | reading.json `format_type ∈ {null, schedule_table, menu_list, notice}` strict closed enum (BUG-044 guard; the `comprehension` value was bleed from `format_role` and is no longer permitted on `format_type`) | 2026-05-17 |
| JA-107 | `data/version.json.counts` declared values equal the actual array length of the referenced corpus file (Cross-Artifact Sync Protocol INV-4 guard; companion to JA-47 which covers the CONTENT-LICENSE.md side) | 2026-05-17 |
| JA-108 | `locales/*.json` key-set parity across all locale files (Cross-Artifact Sync Protocol INV-5 guard; strict full-key equality including `_meta` block) | 2026-05-17 |
| JA-112 | `AUDIO.md` "N listening items use M distinct VOICEVOX speakers" claim matches live data (N == len(listening.json.items); M == |distinct audio_render_meta.voices_used|). Cross-Artifact Sync Protocol INV-4 guard, third instance alongside JA-47 (CONTENT-LICENSE.md) and JA-107 (version.json). BUG-050 charitable close-out 2026-05-17. | 2026-05-17 |
| JA-110 | listening.json items deprecate legacy `voice_planned` field; `audio_render_meta` is the canonical engine + speaker source post the 2026-05-12 VOICEVOX migration (BUG-047 guard) | 2026-05-17 |
| JA-111 | listening.json items drop legacy `format` field; `format_type` ∈ {task_understanding, point_understanding, utterance_expression, immediate_response} strict closed enum (BUG-051 guard) | 2026-05-17 |

### 25.2 Scope discipline (N5 whitelist + OOS guards)

Kanji and vocab scope-rule enforcement.

| ID | Rule (one-line) | Source |
|---|---|---|
| JA-13 | No out-of-scope kanji in user-facing data (grammar / vocab / reading / listening / questions / authentic) | Round-1 |
| JA-16 | Kanji.json `examples` use only the target kanji + N5 whitelist | Round-2 |
| JA-18 | Reading explanation kanji is subset of passage kanji (no novel kanji introduced) | Round-2 |
| JA-20 | Reading choices kanji is subset of passage kanji | Round-2 |
| JA-21 | N4-grammar markers require `tier: late_n5` | Round-3 |
| JA-25 | Whitelist exceptions documented (Pass-22 F-22.4) | Round-4 |
| JA-28 | Dokkai-paper kanji bounded by N5 whitelist + exception list | 2026-05-02 |
| JA-40 | Paper distractors (moji/goi/bunpou) bounded by N5 + paper-distractor exception | 2026-05-08 |
| JA-56 | Corpus sizes locked at 178 / 999 / 106 / 54 / 50 (BUG-018+ updates documented in CHANGELOG; current locked value reflects post-dedup state — see source for exact number) | 2026-05-12 |
| JA-66 | `explanation_en` + `pd_refs.pattern_role` kanji bounded by N5 whitelist | 2026-05-13 |
| JA-72 | Gairaigo vocab entries use katakana in their `form` field | 2026-05-13 |
| JA-99 | All kanji in vocab.json `form` fields are in N5 whitelist OR exception list (BUG-017 guard) | 2026-05-16 |

### 25.3 Pedagogical content quality

No template leaks, no folk-linguistics, no English-only display strings, register variants properly framed, examples natural Japanese.

| ID | Rule (one-line) | Source |
|---|---|---|
| JA-3 | Furigana / catalog match (rendered furigana agrees with reading catalog) | Round-1 |
| JA-9 | Engine display contract (rendered output uses canonical engine keys) | Round-1 |
| JA-10 | No `(see n5-NNN)` redirect text in shipped data | Round-1 |
| JA-14 | No `auto-ruby` code in renderer source | Round-2 |
| JA-17 | Grammar examples have `vocab_id` references (homograph guard) | Round-2 |
| JA-23 | Listening `script` choices match `choices` array | Round-3 |
| JA-24 | i-adj kanji `primary_reading` is the kun-yomi | Round-3 |
| JA-27 | No English `translation`/`title` fields in reading/listening corpora (kana/kanji only at display surface) | 2026-05-02 |
| JA-30 | No past-paper provenance signatures in question text (clean-room hygiene) | 2026-05-02 |
| JA-32 | Paper-JSON rationales appear verbatim in source MD (round-trip integrity) | 2026-05-04 |
| JA-36 | Answer-position distribution per corpus within ±10 pp of even (no test-taking shortcut) | 2026-05-06 |
| JA-38 | Every grammar pattern has ≥1 `common_mistakes` entry | 2026-05-06 |
| JA-51 | Every grammar pattern has ≥3 categorized `common_mistakes` | 2026-05-12 |
| JA-52 | Every grammar pattern has ≥1 `contrasts` with valid `with_pattern_id` | 2026-05-12 |
| JA-53 | Every grammar pattern has non-trivial `cultural_callout` | 2026-05-12 |
| JA-54 | Every grammar pattern essay totals ≥500 chars (Tofugu-bar minimum) | 2026-05-12 |
| JA-55 | Every grammar pattern essay has all 6 sub-fields (intro / why / pitfalls / contrasts / practice / cultural_context) | 2026-05-12 |
| JA-57 | No LH/HL pitch notation in vocab.json (use `{mora, drop}` integer; pitch ≤3-mora bug) | 2026-05-12 |
| JA-58 | No "JLPT.jp official" citations (JEES 出題基準 discontinued 2010) | 2026-05-12 |
| JA-59 | No competitive gamification keys / XP / leaderboards / badges (per privacy posture) | 2026-05-12 |
| JA-60 | No account / cloud-sync code paths (no fetch to non-local URLs) | 2026-05-12 |
| JA-61 | No per-content discussion / comments route | 2026-05-12 |
| JA-62 | No romaji in user-facing Japanese display fields | 2026-05-12 |
| JA-63 | Authentic `kanji_refs` lists all N5 kanji in `ja` text (no missing cross-refs) | 2026-05-12 |
| JA-65 | `contrasts.notes` ≥30 chars (substantive explanation bar) | 2026-05-12 |
| JA-71 | Grammar `meaning_ja` first 「marker」 must overlap with `pattern` field | 2026-05-13 |
| JA-75 | Grammar `meaning_ja` must contain ≥1 of its pattern's `_meaning_ja_markers` | 2026-05-13 |
| JA-76 | Kanji on-yomi entries written in katakana (pedagogical convention) | 2026-05-13 |
| JA-77 | No placeholder / TODO / "unspecified" text in shipped data fields | 2026-05-13 |
| JA-78 | Grammar example sentences avoid 3+ consecutive same hiragana (clarity) | 2026-05-13 |
| JA-81 | No grammar example sentence repeated in 10+ patterns (boilerplate-leak guard) | 2026-05-14 |
| JA-83 | No vocab example uses removed templates (`Xを見ました` / `あのXはどこ` / `これはXです` / `Xとあいさつしました`) | 2026-05-15 |
| JA-84 | kanji.json `sentences` have populated `translation_en`; no `<u>` markup leakage | 2026-05-15 |
| JA-88 | Particle-precision L2-error scan across all corpora (wave-2) | 2026-05-15 |
| JA-89 | Native-teacher audit locks: counter + article + animacy + template phases A/B/D | 2026-05-15 |
| JA-96 | No bare `<form>が あります/います` template examples in vocab.json (BUG-014 guard) | 2026-05-16 |
| JA-102 | Standalone-kun `primary_reading` lock for 人/中/外/東/車/国 (BUG-021 guard) | 2026-05-17 |
| JA-140 | Moji `stem_html` uses HTML `<u>...</u>` emphasis wrapper, not markdown `__...__` (MOJI-001 drift guard; 100/100 moji stems normalized to HTML 2026-05-21) | 2026-05-21 |
| JA-141 | Moji `grammarPatternId` non-null must NOT have `provenance="auto_inferred"` (MOJI-002 drift guard; 28 spurious IDs scrubbed; moji tests orthography, not grammar — same anti-pattern class as the n5-013 over-misuse fixed at PAPER-001) | 2026-05-21 |
| JA-144 | REVIEW_DATE lines in `data/n5_kanji_whitelist.exceptions.md` use ISO 8601 YYYY-MM-DD format (DOCS-KANJI-004 close-out, 2026-05-21). Skips template values inside HTML comments (`<!-- ... -->`) since template-block entries aren't live. Currently 0 live entries (file in bootstrapping mode pending `data/n5_official_kanji_scope.json`); invariant pre-activated so the first real exception entry must comply. | 2026-05-21 |
| JA-145 | Every `data/papers/<cat>/paper-N.json` carries `source_file` as either (a) a path that resolves to an existing repo file under `N5/`, OR (b) the literal canonical sentinel `"(authored in-place)"` (DOCS-VOCAB-005 close-out, 2026-05-22). Any other value fails — including parenthesized prose other than the canonical sentinel (e.g., the pre-fix `"(authored in-place; was KnowledgeBank/<x>_questions_n5.md before merge on 2026-05-14)"` value which embedded a now-stale historical breadcrumb in 28 paper files). Canonical-sentinel pattern: define a literal sentinel + parallel resolvable-path branch on data-metadata fields that represent provenance of authored-in-place content; CI locks the sentinel-or-path invariant. Closes the unaddressed half of DOCS-VOCAB-003 (which prematurely closed as a README-only fix). | 2026-05-22 |
| JA-146 | xlsx `Fix Commit` cell on every Fixed row in `specifications/test-scenarios-by-specialist-perspective.xlsx` is either (a) a commit-hash-shaped string (7-40 hex chars, optionally followed by `(+ submodule HASH)`); or (b) a `<...>`-shaped sentinel string acknowledging hash absence with explanatory prose. Rejects datetime objects, bare date strings (YYYY-MM-DD), empty cells, and free text. Closes the GAP-A type-confusion class surfaced 2026-05-22 when a meta-audit found 150/155 Fixed rows had date values in col 10 (99 as Excel-coerced datetime objects, 51 as YYYY-MM-DD strings) rather than commit hashes — JA-118 had only checked non-emptiness. After the one-time cleanup pass (`tools/cleanup_fix_commit_cells_2026_05_22.py`) sentinelized the redundant dates, this invariant locks the convention. **Reactive→proactive lesson** (procedure manual §F.42): existing CI invariants catch patterns we'd already named; coverage gaps for unnamed pattern classes surface only via meta-audit. | 2026-05-22 |
| JA-147 | whitelist `data/n5_vocab_whitelist.json` ↔ vocab.json mismatch set (tokens in whitelist not matching any vocab.json form/reading) MUST equal the "Known mismatches" enumeration documented in `data/n5_vocab_whitelist_README.md`. Catches the drift class where the README's known-mismatch list falls out of sync with the actual computed mismatch set. Header count + bulleted backticked tokens both verified. Closes DOCS-VOCAB-006 (BUG-156, 2026-05-22). | 2026-05-22 |
| JA-148 | Every `data/grammar.json` entry's `id` must appear in EITHER `core_n5` OR `late_n5` OR `deferred_to_n4` in `data/n5_core_pattern_ids.json`. AND: entries classified `deferred_to_n4` must have `scope: 'n4'`; entries classified `core_n5` or `late_n5` must have `scope: 'n5'` or no `scope` field (n5 is the default). Closes DOCS-CORE-001 (BUG-157, 2026-05-22) — previously the 5 deferred-to-N4 entries (n5-144, n5-157, n5-158, n5-175, n5-176) lacked a `scope` marker, so downstream consumers couldn't distinguish them from in-scope N5 patterns. | 2026-05-22 |
| JA-149 | Every entry in `data/dokkai_kanji_exception.json` `exception_kanji` list must have a non-empty `reason` field that does NOT contain the placeholder phrases "Pre-formalization" or "rationale not individually recorded". Closes DOCS-DKE-001 (BUG-160, 2026-05-22) — backfill landed 25 specific per-kanji rationales replacing the placeholder boilerplate; this invariant prevents re-introduction when new exception entries are added. | 2026-05-22 |
| JA-150 | Every kanji that appears in any `data/vocab.json` example's `ja` field must be in EITHER `n5_kanji_whitelist` OR `dokkai_kanji_exception`. Brings vocab examples up to parity with grammar examples / reading passages / listening scripts (all of which were already gated against the same predicate). Closes NTR-001 (BUG-161, 2026-05-22) — the native-teacher review found 99 of 3,036 vocab examples leaked non-whitelist kanji (LLM-curation pass ran ~3× the human-baseline violation rate). The rewrite landed via mechanical kana-substitution of side-words; this invariant locks the predicate going forward. | 2026-05-22 |

### 25.4 Cross-corpus & cross-file integrity

References resolve, forms match, IDs stay stable, derived data agrees with source.

| ID | Rule (one-line) | Source |
|---|---|---|
| JA-4 | Vocab `reading` uniqueness | Round-1 |
| JA-7 | No duplicate stems in question file | Round-1 |
| JA-12 | Kanji KB / JSON consistency | Round-1 |
| JA-15 | Audio refs resolve to files on disk | Round-2 |
| JA-22 | Kanji `kun` readings deduplicated within an entry | Round-3 |
| JA-26 | No duplicate question IDs across questions / drills / papers | 2026-05-02 |
| JA-31 | Vocab PoS tags in vocabulary_n5.md agree with data/vocab.json | 2026-05-02 |
| JA-34 | Core-N5 vs late-N5 split agrees with grammar.json `tier` field | 2026-05-05 |
| JA-67 | Density-3 (kanji→vocab) below-floor count locked (drops below floor = regression) | 2026-05-13 |
| JA-70 | Vocab `pitch_accent.mora == count_mora(reading)` for single-reading entries | 2026-05-13 |
| JA-82 | `_meta.see_also` / `_meta.consumers` path references resolve to actual files | 2026-05-14 |
| JA-87 | Cross-corpus reading / gloss consistency vs vocab.json canonical (wave-1) | 2026-05-15 |
| JA-90 | Vocab `pitch_accent.drop` validated vs kanjium reference (810 high-confidence matches, 199 unverified) | 2026-05-15 |
| JA-100 | kanji.json compound/example.form == linked vocab.form **STRICT** (BUG-020 OOS + BUG-023 in-scope drift; tightened from narrow to strict 2026-05-17) | 2026-05-17 |
| JA-103 | kanji.json `n5_compounds`: `(form, reading)` tuple unique within each kanji entry (BUG-024 guard; legitimate polysemy with different readings — e.g., 一日 ついたち vs いちにち — PASSES) | 2026-05-17 |
| JA-105 | reading.json `vocab_preview` is a list of vocab_id strings; each ID resolves against vocab.json (BUG-045 guard; embedded-dict denormalization shape rejected to avoid stale-snapshot drift) | 2026-05-17 |
| JA-109 | Procedure manual + prompts → `tools/*.py` script references resolve to actual files on disk (Cross-Artifact Sync Protocol INV-10 guard; scope: N5 prompts + AUDIT-COVERAGE-*.md only — the cross-level procedure manual is intentionally excluded because its script refs are abstract Nx-builder targets) | 2026-05-17 |
| JA-113 | Meta-route static mirrors (`home/`, `changelog/`, `privacy/`, `notices/`) reflect the latest H1/H2 from their source markdown (README.md / CHANGELOG.md / PRIVACY.md / NOTICES.md). Cross-Artifact Sync Protocol INV-7 extension — derived-mirror freshness vs source. Wired after 3 instances of the meta-route mirror drift class in the 2026-05-17 session. | 2026-05-17 |
| JA-114 | listening.json `pacing_status ∈ {in_range, too_slow, too_fast, no_audio, unmeasured}` — strict closed enum (BUG-048/049 follow-up). Same class as JA-106 / JA-111 (closed-enum on a corpus field where the value-domain is small and stable). | 2026-05-17 |
| JA-115 | README.md "Content (current as of ...)" section count claims (`N grammar patterns`, `M vocabulary entries`, `K N5 kanji`, etc.) match live data. Fourth instance of Cross-Artifact Sync Protocol INV-4 class alongside JA-47 (CONTENT-LICENSE.md), JA-107 (version.json), JA-112 (AUDIO.md). | 2026-05-17 |
| JA-116 | Every A-NN audit category / Phase-0 regression block / FP-NN false-positive class in `N5/prompts/*` has ≥1 matching xlsx scenario row (auto-extracted; word-boundary match). Cross-Artifact Sync Protocol INV-6 promotion to Wired (2026-05-17). | 2026-05-17 |
| JA-117 | Cross-corpus `passage_id` (kanji.json `entries[*].reading_passages[*].passage_id` → reading.json, 363 refs) + `pattern_id` (reading.json `passages[*].grammar_footnotes[*].pattern_id` + nested, 319 refs → grammar.json) resolve to actual entries. Cross-Artifact Sync Protocol INV-7 extension; promotes INV-7 to Wired. | 2026-05-17 |
| JA-118 | Every Fixed-status row in xlsx User Reported Bugs has a non-empty Fix Commit cell. Cross-Artifact Sync Protocol INV-9 promotion to Wired (2026-05-17). Companion tool: `tools/populate_bug_fix_commits_2026_05_17.py` (scans git log for commit subjects mentioning each BUG-NNN, supports range patterns). | 2026-05-17 |
| JA-92 | No `translation_en` string repeated in 10+ grammar examples (parallel to JA-81 which catches the JA-side boilerplate; BUG-003/005 lineage). Wired 2026-05-17 from the reserved JA-91..95 range. | 2026-05-17 |
| JA-93 | Vocab.json `pitch_marks` total mora count matches `count_morae(reading)` for every entry that carries pitch_marks. BUG-004 lineage; algorithmic mora-count guard preserved from `not-required/tools-archive/fix_issue_074_pacing_audit_2026_05_06.py`. Wired 2026-05-17. | 2026-05-17 |
| JA-95 | Particle-pattern alignment: for grammar patterns in category "Particles" whose `pattern` field is a single-character particle (は, が, を, etc., ≤ 2 chars), every example's `ja` must contain that particle. BUG-009 lineage. First-run wire-up caught n5-028 ex[5] (の particle) using は instead — fixed inline 2026-05-17 (ja `父は 先生です。` → `わたしの 父は 先生です。`). Wired 2026-05-17. | 2026-05-17 |
| JA-91 | No cross-pattern `explanation_en` similarity ≥0.85 (Levenshtein-style SequenceMatcher ratio). BUG-003 contamination guard. Wired 2026-05-17 + Phase B resolution same day: all 43 prior pairs (DUPLICATE_PATTERN ×8, CROSS_REFERENCE ×21, ALTERNATIVE_VARIANT ×12, SUBSET ×2) addressed via explanation_en rewrites on the deferring side (or both sides for ALTERNATIVE_VARIANT) so each falls below the threshold. `data/_ja91_baseline.json` now carries empty `baseline_pairs` as a RESOLVED snapshot documenting the prior pair set + classification legend. JA-91 enforces the threshold unconditionally; trips on any newly-introduced similar-explanation pair. | 2026-05-17 |
| JA-94 | Every grammar example's `ja` contains ≥1 structural marker from its parent pattern's marker list in `data/pattern_markers.json` (178-pattern catalog auto-derived by `tools/author_pattern_markers_2026_05_17.py`). BUG-006 pattern-instance contamination guard. Wired 2026-05-17 + Phase A resolution same day: all 14 prior BUG-006-CANDIDATE wrong-example failures replaced with parent-pattern-demonstrating examples (n5-030 ×3 / n5-048 ×3 / n5-065 ×1 / n5-071 ×1 / n5-084 ×1 / n5-112 ×1 / n5-157 ×3 / n5-164 ×1). `data/_ja94_baseline.json` now carries empty `baseline_failing_examples` as a RESOLVED snapshot. JA-94 enforces marker-presence unconditionally across all 1782 examples; trips on any newly-added example that doesn't demonstrate its parent pattern. | 2026-05-17 |
| JA-119 | Spec §7.3 sample `version.json` block (a JSON example shown to developers) must match the live `data/version.json` `counts` field exactly. Cross-Artifact Sync Protocol INV-4 — fifth surface locked alongside JA-47 (CONTENT-LICENSE.md) / JA-107 (version.json) / JA-112 (AUDIO.md) / JA-115 (README.md). Wired after BUG-050 was filed three times against the actual version.json file even though the file was clean — auditor was reading the stale v1.12.50-era sample in the spec (vocab 1041, listening 47, etc.) and mistaking it for current state. | 2026-05-17 |
| JA-120 | Paper bunpou Mondai-1 `grammarPatternId` matches canonical particle pattern (PAPER-001 drift guard). For every Mondai 1 question in `data/papers/bunpou/paper-*.json` whose `correctIndex` resolves to a single Japanese particle (the canonical 21-entry map: は→n5-002, が→n5-003, を→n5-004, に→n5-005, へ→n5-006, で→n5-007, と→n5-008, から→n5-009, まで→n5-010, や→n5-011, も→n5-013, か→n5-023, ね→n5-025, よ→n5-026, の→n5-028, だけ→n5-033, ぐらい/くらい→n5-035, ごろ→n5-036, など→n5-037, ずつ→n5-038, より→n5-095), the `grammarPatternId` must reference that pattern. Caught 30+ questions tagged `n5-013` (も) regardless of actual answer particle. Wired 2026-05-18. | 2026-05-18 |
| JA-121 | Paper-question `rationale` / `rationale_hi` free of commit-message-style meta-fix history (PAPER-003 drift guard). Substring-scans 12 trigger phrases (`auto_inferred`, `previously tagged`, `prior version was`, `Stem now anchored`, `Stem now includes`, `replaces colloquial`, `replaces ので per`, `the original option`, `was dropped because`, `replaced with`, `patched to`, `fix:`). Distractor-analysis content preserved by design (no trigger overlap). Wired 2026-05-18. | 2026-05-18 |
| JA-122 | Paper-question `rationale_hi` free of English-pattern fragments (PAPER-004 drift guard). Substring-scans 17 trigger fragments covering apostrophe-s possessive (`'s घर`, `दोस्त's`, `माता's`), English contractions (`मैं'm`, `मैं'll`), mojibake artifacts (`यहाँre`, `वहाँre`, `o'घड़ी`, `o'क्लॉक`), and untranslated English filler (` yet,`, `है yet`, ` lot `, `I am `, ` have जाना`). Wired 2026-05-18. | 2026-05-18 |
| JA-123 | Every `data/papers/*/*.json` paper-pack file has a corresponding static HTML mirror at `/papers/<id>/index.html`, plus the `papers/index.html` landing page. LLM-001 (BUG-094) drift guard — without per-paper mirrors, LLM crawlers and search engines cannot read paper content (hash-routed SPA shell returns same page for every URL). Cross-Artifact Sync Protocol INV-LLM-1. Wired 2026-05-18. | 2026-05-18 |
| JA-124 | `sitemap.xml` has ≥1000 `<loc>` entries — regression floor for LLM-002 (BUG-095) close-out. Pre-fix state was 10 meta-route URLs only; post-fix is 1589 URLs covering every static mirror + 7 summary pages + 11 meta routes + paper mirrors. INV-LLM-2. Wired 2026-05-18. | 2026-05-18 |
| JA-125 | Every entry in `data/index.json` has `size_bytes` matching the actual on-disk file size. LLM-003 (BUG-096) drift guard — same INV-4 / JA-107 corpus-count drift class extended to the new corpus discovery catalog. INV-LLM-3. Wired 2026-05-18. | 2026-05-18 |
| JA-126 | The 7 LLM-005 (BUG-105) thin summary pages (`home.html`, `grammar.html`, `vocabulary.html`, `kanji.html`, `reading.html`, `listening.html`, `test.html`) all exist at the N5 root, plus `llms.txt` exists at both `/JLPTSuccess/` (root) and `/JLPTSuccess/N5/` for crawler discovery. INV-LLM-5. Wired 2026-05-18. | 2026-05-18 |
| JA-127 | No entry in `wrong_corrected_pair` with `error_category == "register"` may have a wrong-field parenthetical naming the register the form is appropriate for. REG-001 (BUG-106) D6 self-contradiction guard. Trip phrases: `(formal)`, `(in formal context)`, `(in formal speech)`, `(polite)`, `(in polite contexts)`, `(casual)`, `(in casual conversation)`, `(among friends)`, `(with intimates)`, `(acceptable in X)`. Internally contradictory entries (the wrong form is annotated as appropriate to a register) must migrate to `common_mistakes` with `kind: register_variant`. INV-REG-D6. Wired 2026-05-18 — first run after the n5-046 close-out caught 5 more entries (n5-097/102/127/173/179) all migrated in the same commit. | 2026-05-18 |
| JA-128 | No paper question may carry a `passage_text` field — canonical text lives in `passages[label].text` at the paper top-level, referenced via the `passage_label` foreign key. DOKKAI-001 (BUG-107) drift guard — caught 102 dokkai questions with redundant per-question copies (12 of which had already drifted via a leading `> ` markdown-blockquote prefix on one copy but not the other) + 10 bunpou Mondai-3 questions (horizontal sweep). Same drift-risk class as KANJI-001/004 / VOCAB-002 / LISTEN-001 (data in two places → drift). INV-PAPER-A. Wired 2026-05-18. | 2026-05-18 |
| JA-129 | Paper `rationale_hi` must be free of untranslated English temporal/quantity markers. Trigger substrings: ` ago `, ` ago.`, ` ago,`, ` ago)`, ` ago]`, ` yet `, ` yet.`, ` yet,`, ` yet)`, ` lot `, ` lot.`, ` lot,`. DOKKAI-002 (BUG-108) drift guard — extends JA-122 PAPER-004 fragment scan (apostrophe-s / contractions / mojibake) to cover temporal-marker carry-overs. Caught dokkai-1.1 (the original target) + goi-7.1 (horizontal sweep). Conservative: skips ` before ` and ` then ` which can appear in legitimate technical glossing. INV-PAPER-B. Wired 2026-05-18. | 2026-05-18 |
| JA-130 | Every paper question has `grammarPatternId` as a key. When the value is `null`, `grammarPatternId_provenance` must start with `not_applicable_` documenting the reason (e.g., `not_applicable_comprehension` for dokkai, `not_applicable_vocab` for goi, `not_applicable_orthography` for moji). DOKKAI-003 (BUG-109) schema-shape guard — same always-a-key-sometimes-null pattern as VOCAB-002 counter-field (BUG-015 close-out). Caught 24 missing in dokkai + 11 in goi + 72 in moji (83 horizontal-sweep fills) = 107 questions total brought to schema consistency. INV-PAPER-C. Wired 2026-05-18. | 2026-05-18 |
| JA-131 | `locales/en.json` + `locales/hi.json` both carry the `nav.all_levels` key. MOB-007 (BUG-116) drift guard — caught a hard-coded English literal `← All JLPT levels` in `js/home.js` that stayed English when locale=hi. Fix: added the key to both locales (Hindi: `सभी JLPT स्तर`) + updated home.js to use `t('nav.all_levels')`. Same shape as JA-108 (broader locale-key-set parity); JA-131 is the narrower invariant for this specific key. INV-MOB-D. Wired 2026-05-19. | 2026-05-19 |
| JA-132 | `css/main.css` AND `css/main.min.css` both carry the MOB-001..016 mobile-UI compliance batch — marker comment `MOB-001..016 mobile UI compliance batch` AND `min-height: 44px` rules on `.btn-action`, `.study-order-link`, `.home-up-link a`, `.toc-expand-all`, `.brand-link`, `.skip-link`, `.btn-tiny`. MOB-002/003/004/005/011/012/013/014/015/016 (BUG-111..125) drift guard — catches accidental removal during a CSS-cleanup pass. INV-MOB-A. Wired 2026-05-19. | 2026-05-19 |
| JA-133 | `css/main.css` contains the rule `input, textarea, select { font-size: max(1rem, 16px); }` (or equivalent ≥16px font-size on these selectors). MOB-006 (BUG-115) iOS Safari auto-zoom guard — when a focused form control has font-size < 16px, iOS Safari zooms the viewport and leaves the page zoomed-in. INV-MOB-B. Wired 2026-05-19. | 2026-05-19 |
| JA-134 | `js/home.js` must NOT use `href="#/levels"` (silently redirects to root then triggers first-run onboarding to `#/diagnostic`); should use `href="../"` directly. `js/listening-story.js` must NOT use `"#/listening/story..."` references (silently redirects to listening index because the router maps `listeningstory` (no slash)); should use canonical `#/listeningstory...`. MOB-008/009 (BUG-117/118) drift guard. INV-MOB-C. Wired 2026-05-19. | 2026-05-19 |
| JA-135 | Every internal `href="#/X"` hash-route reference in `js/*.js` (excluding `js/app.js` and `js/min/*`) must point to a route whose name appears in app.js's routes dict (extracted via regex on the canonical `name: render<X>` mapping). General hash-route-resolution guard — extends JA-134 from "2 specific known patterns" to "every internal hash-href resolves". Catches future MOB-008/009-class dead-end introductions without per-pattern hardcoding. INV-MOB-C-EXT. Wired 2026-05-19. | 2026-05-19 |
| JA-136 | No `rationale_hi` value (longer than 30 chars) may be shared verbatim by 2+ questions within the same paper file. GOI-001 (BUG-130) copy-paste guard — caught goi-6.11's rationale_hi being a byte-identical copy of goi-6.12's (about a different question topic). The bug spec's stricter proposal (rationale_hi must share a Japanese token with stem/correctAnswer) was REJECTED for ~100 false positives on the existing corpus (dictionary-form vs polite-form variation). Cross-question duplication is the narrower-but-defensible proxy; semantic token equivalence would require morphological stemming. INV-PAPER-D. Wired 2026-05-19. | 2026-05-19 |
| JA-137 | No off-by-one `rationale_hi` shift across consecutive questions. GOI-001 + GOI-004 (BUG-130 + BUG-133) drift guard. Narrow detector: a question's rationale_hi has 0 Japanese-token overlap with its OWN stem+answer AND ≥2 token overlap with the NEXT question's stem+answer. The asymmetric overlap is the actual off-by-one signal; absolute overlap is too noisy as a CI gate (~21% false positives from polite-form ↔ dictionary-form variation). Sharper detector → cleaner gate; false-positive rate <1% on the live corpus. Broader same-question token-overlap divergence remains advisory in `tools/audit_rationale_overlap_2026_05_21.py`. INV-PAPER-E. Wired 2026-05-21. | 2026-05-21 |
| JA-139 | No Devanagari letter embedded inside a JP-character word in `rationale_hi`. GOI-006 (BUG-135) mojibake guard. Detector: regex `[ぁ-ゖァ-ヺ一-鿿][ऀ-ॣ०-ॿ]` — Devanagari letter immediately following a kana/CJK character with no separator. Exclusions: Devanagari danda `।` (U+0964) and double-danda `॥` (U+0965), which are legitimate sentence-end punctuation; hyphen-separated cross-script terms like 「い-विशेषण」. JA-138 number intentionally skipped — JA-121's trigger set was extended in place (not minted as a new invariant) to cover GOI-005's additional fix-history phrases; the underlying anti-pattern is unchanged so the existing JA-NN holds. Horizontal-deployment sweep on this invariant surfaced 2 more dokkai mojibake instances (dokkai-2.11 + dokkai-3.4) that the original goi-only filing missed — both fixed in the same close-out batch. INV-PAPER-F. Wired 2026-05-21. | 2026-05-21 |

### 25.5 Locale parity & i18n

Hindi-locale coverage, particle-handling in non-JA prose.

| ID | Rule (one-line) | Source |
|---|---|---|
| JA-41 | Hindi prose: Japanese grammatical particles attached to Hindi terms must be in kana (R-1.1 rule) | 2026-05-07 |
| JA-85 | Dokkai locale + `format_role` parity (`explanation_hi` populated where `explanation_en` is) | 2026-05-15 |
| JA-86 | authentic `context_hi` + questions.json distractor en/hi coverage parity | 2026-05-15 |
| JA-142 | No over-literal Hindi `के पास है पढ़ते हुए` substring in `rationale_hi` (MOJI-005 word-by-word translation guard; same defect class as DOKKAI-002 `ago` / DOKKAI-004 `by` / PAPER-004 `yet`/`lot` fragment scans — translation-pattern leak where English construction has no direct Hindi cognate). Trip: `'के पास है पढ़ते हुए' in rationale_hi`. Fix pattern: rewrite as natural Hindi `X का पठन Y है`. | 2026-05-21 |
| JA-143 | Rationale / rationale_hi character-count parity within ~0.6× – 2.0× ratio for entries where EN ≥ 80c AND HI ≥ 40c (MOJI-006 content-coverage truncation guard). Accounts for HI's typical 1.3× expansion vs EN. Below-0.6 trips as "HI may be truncated"; above-2.0 trips as inverse ("EN may be truncated"). Skips minimalist rationales (EN < 80c) and missing/one-token HI (< 40c) which are pre-existing intentional brevity. | 2026-05-21 |

### 25.6 Legal / license / attribution

CONTENT-LICENSE.md accuracy, third-party attribution preservation, public-domain reference legal status.

| ID | Rule (one-line) | Source |
|---|---|---|
| JA-37 | localStorage namespace in code matches PRIVACY.md verbatim | 2026-05-06 |
| JA-47 | CONTENT-LICENSE.md corpus counts agree with live data/*.json counts (legal-vetting F-3) | 2026-05-11 |
| JA-48 | KanjiVG SVG copyright headers preserved (Ulrich Apel attribution per CC-BY-SA 3.0) | 2026-05-11 |
| JA-68 | Cache-version sync: index.html `css?v=` + `js?v=` + sw.js `CACHE_VERSION` all match | 2026-05-13 |
| JA-69 | `public_domain_refs` legal-status guard: no 'pending' / 'protected' `pd_status`; `author_death_year` ≤ 1955 buffer | 2026-05-13 |

### 25.7 Reserved / not-yet-wired

Documented in the audit-coverage doc as ready-to-wire follow-ons.
Currently implemented as Phase-0 regression-block scripts in
`prompts/N5Improvement.txt` rather than hard CI gates.

| ID | Planned rule | Origin |
|---|---|---|
| JA-42 | Reserved | — |
| JA-43 | Reserved | — |
| JA-44 | Reserved | — |
| JA-45 | Reserved | — |
| JA-46 | Reserved | — |
| JA-80 | (attempted then retired — heuristic meaning_ja substring check had 19 false positives; semantic comparison stays in manual-review domain) | 2026-05-13 |

**JA-91 + JA-94 final-unblocked + fully resolved 2026-05-17** (moved
out of Reserved into §25.4 row entries; both run with EMPTY baselines
post-Phase-A and Phase-B same-day):
- JA-91 — `data/_ja91_baseline.json` was authored with 43 pairs (DUPLICATE_PATTERN ×8 / CROSS_REFERENCE ×21 / ALTERNATIVE_VARIANT ×12 / SUBSET ×2); Phase B rewrote the deferring side of each pair so all 43 fell below the 0.85 threshold. The baseline file is retained with `baseline_pairs: []` as a RESOLVED snapshot.
- JA-94 — `data/pattern_markers.json` (178-pattern structural-markers catalog) + Phase A's 14 wrong-example replacements (in n5-030 / n5-048 / n5-065 / n5-071 / n5-084 / n5-112 / n5-157 / n5-164). The baseline file is retained with `baseline_failing_examples: []` as a RESOLVED snapshot.

### 25.8 Class-and-bug lineage

Many invariants were added in response to user-reported bugs.
Cross-reference table:

| Invariant | Triggered by | Bug class |
|---|---|---|
| JA-96 | BUG-014 | Template-generated semantic-nonsense vocab examples |
| JA-97 | BUG-015 | Inconsistent counter field schema |
| JA-98 | BUG-016 | Transitivity coverage gap on verbs |
| JA-99 | BUG-017 | Vocab `form` field with OOS kanji |
| JA-100 | BUG-020 + BUG-023 | kanji.json ↔ vocab.json form drift (both directions) |
| JA-101 | BUG-022 | kanji.json examples `lemma` vs `form` schema split |
| JA-102 | BUG-021 | `primary_reading` set to on-yomi for kun-yomi-standalone kanji |
| JA-103 | BUG-024 | Duplicate compounds within kanji.json (subset-gloss duplicates auto-derived from pre-dedup vocab.json) |
| JA-104 | BUG-041 | Reading.json `level` field carried 4 mixed-semantics values; renamed to `difficulty` with closed enum |
| JA-105 | BUG-045 | Reading.json `vocab_preview` had two shapes (list-of-strings vs list-of-dicts) across authoring batches; normalized to list-of-vocab_id-strings |
| JA-106 | BUG-044 | Reading.json `format_type` and `format_role` both held the passage-type value `"comprehension"` on the same passages; `format_type` re-scoped to info-search visual subtype enum |
| JA-107 | (drift discovered during Rule-5 install) | `version.json.counts.vocab` carried the pre-dedup 1009 value while `vocab.json` had been reduced to 995 entries (residue of BUG-018/019/024 dedup batches that didn't propagate to the version manifest); fixed in the same commit as the JA-107 wiring |
| JA-108 | (governance) | Cross-Artifact Sync Protocol INV-5; the en.json `_meta` block was missing (asymmetric with hi.json) and 6 chokai_detail keys were missing from hi.json; both gaps closed in the install commit |
| JA-109 | (governance) | Cross-Artifact Sync Protocol INV-10; N5Improvement.txt referenced a deleted script (`tools/register_dev_issue_list_deferrals_2026_05_05.py`); reference updated to the still-extant `tools/register_audit_2026_05_12.py` in the install commit |
| JA-110 | BUG-047 | listening.json `voice_planned.engine="edge-tts"` contradicted `audio_render_meta.voice_provider="voicevox"` on all 50 items after the 2026-05-12 VOICEVOX migration; voice_planned dropped (audio_render_meta canonical); JS UI re-wired |
| JA-111 | BUG-051 | listening.json `format` and `format_type` were 1:1 redundant across all 50 items; format dropped; format_type canonical (closed enum). Same dual-field-redundancy class as JA-106 (BUG-044) |
| JA-112 | BUG-050 (charitable interpretation) | AUDIO.md claimed "47 listening items use 4 distinct VOICEVOX speakers" — stale prose carried over after 2026-05-12 VOICEVOX render (live: 50 items / 6 speakers). Bug-report description mis-located the drift to version.json (which has been 50 in every observable instance); AUDIO.md was the actual stale source. Speaker-table character names also corrected (same drift class as BUG-053). |
| JA-113 | (governance — recurring drift caught in 2026-05-17 session) | Three instances observed in one session of CHANGELOG.md edits not propagating to the changelog/index.html static mirror in the same commit (cdef185 → f96475b correction; 5d14cde + 47d1edc → 360eb74 correction). Pattern: maintainer edits markdown source under N5/ but doesn't re-run `tools/build_static_mirrors.py --stages meta`. JA-113 prospectively catches the class for home/changelog/privacy/notices routes. |
| JA-64 (tightened) | BUG-011 + BUG-013 | Register-variant common_mistakes schema migration |
| JA-35 (extended enum) | BUG-012 | `review_status` provenance disambiguation |
| JA-140 | MOJI-001 (BUG-139) | Stem-emphasis convention split: Mondai 1 used HTML `<u>X</u>` (50/50); Mondai 2 used markdown `__X__` (50/50); paper-4 mixed both at the Mondai 1→2 boundary. Renderer-fragility class — an HTML-only renderer displays markdown literally and vice versa. Normalized to HTML for all 100 moji questions; JA-140 prevents future per-mondai split. |
| JA-141 | MOJI-002 (BUG-140) | 28 moji questions carried `grammarPatternId` from `auto_inferred` matching surface tokens (e.g., `こども` → n5-013 because も appears; `今日` → n5-117 because は appears). Same anti-pattern class as the n5-013 over-misuse fixed at PAPER-001 in the bunpou paper sweep — auto-inference treats orthography stems as if they tested grammar. JA-141 forbids the `non-null + auto_inferred` combination on moji-category questions, forcing manual sign-off when a moji question legitimately tests a pattern. |
| JA-142 | MOJI-005 (BUG-143) | 2 questions had `rationale_hi` with over-literal Hindi rendering `के पास है पढ़ते हुए` (word-by-word "has reading"). Same defect class as DOKKAI-002 (`एक महीना ago`) / DOKKAI-004 (`आना-जाना by ट्रेन`) / PAPER-004 (`यहाँre` / `o'घड़ी` etc.) — translation pattern leak where English construction has no direct Hindi cognate. JA-142 is the substring guard; corpus-wide fix pattern is `X का पठन Y है`. |
| JA-143 | MOJI-006 (BUG-144) + 4 same-class follow-ups | `moji-7.2` rationale_hi truncated 250c vs EN 343c — dropped the EN conclusion "for N5 the 立 form is the only correct match." Content-coverage parity, not translation-quality. After wiring JA-143, 4 more pre-existing instances surfaced (goi-7.9 / moji-1.6 / moji-4.10 / moji-6.3); all extended in the same batch. JA-143 guards against future truncation: rationale_hi must be within 0.6×–2.0× of rationale length. |
| JA-144 | DOCS-KANJI-004 (BUG-150) | `n5_kanji_whitelist.exceptions.md` REVIEW_DATE field had no documented format; downstream parsing risk. JA-144 enforces ISO 8601 (YYYY-MM-DD); skips template values inside HTML comments. Pre-activated at 0 live entries; first real exception entry must comply. Pattern: regex-validate format on optional fields in governance docs to prevent downstream parsing fragility. |

For the full bug-class lineage (BUG-001 through BUG-145) and the
authoring rules that prevent recurrence, see Appendix F (sections
F.15 through F.24) of `JLPT Common/procedure-manual-build-next-jlpt-level.md`.
BUG-049 (pacing too slow on 26/50 listening items) stays Open
this batch; needs audio re-render at VOICEVOX speed_scale ~1.3.

### 25.9 Adding a new invariant — workflow

When closing a bug class that warrants a CI gate:

1. **Define the rule.** Write a `_check_ja_NN_*()` function in
   `tools/check_content_integrity.py`. Each function returns a list
   of failure-message strings (empty list = PASS).
2. **Register the rule.** Add a `("JA-NN", "description", lambda: ...)`
   tuple to the registry list near the top of `main()`.
3. **Pick the next free number.** Use the highest existing JA-NN + 1
   (currently next free = JA-120). Reserved slots (JA-42..46, JA-80)
   must NOT be reused. (JA-91 / JA-92 / JA-93 / JA-94 / JA-95 / JA-119
   were all wired 2026-05-17 — the JA-91..95 range is fully consumed;
   see §25.4 rows.)
4. **Validate on the current corpus** — run the script. The new
   invariant must PASS on the post-fix corpus, otherwise the fix is
   incomplete.
5. **Mirror in this section (25).** Add a row to the appropriate
   subsection (25.1..25.6) with the same description text.
6. **Cross-reference in the bug close-out commit message** + the
   `AUDIT-COVERAGE-YYYY-MM-DD.md` addendum's "CI gate" line.

Default to **strict equality / closed-enum checks**, not narrow
sub-scope checks. The BUG-020 → BUG-023 round-trip demonstrated that
narrow-by-default gates miss real bug classes — false negatives don't
ring CI bells, but they're still bugs. Loosen only with documented
exceptions when a real false positive surfaces.

### 25.10 Cross-Artifact Sync Protocol — INV↔JA mapping (added 2026-05-17)

BINDING Rule 5 (`JLPTSuccess/.claude/CLAUDE.md`) installs the
Cross-Artifact Synchronization Protocol — when ONE artifact changes,
every OTHER artifact that references the changed thing updates in
the same commit. The protocol defines INV-1..INV-10 as build-time
guards; this subsection maps each INV-N to its concrete JA-NN
implementation in `tools/check_content_integrity.py` or to its
current convention-only / partial / out-of-scope status.

The operational handbook (concrete file map per artifact class,
dependency matrix, commit-time checklist) lives in
`N5/docs/cross-artifact-sync-map.md`. This section is the spec-side
contract.

| INV | Description | Implementation | Status |
|---|---|---|---|
| INV-1 | Bug-fix commit touches a test file or annotates "no test" | **`.githooks/commit-msg`** (rejects bug-fix commit messages that lack a test / regression / JA-NN / "no test — reason" annotation). Install: `git config core.hooksPath .githooks`. | Convention + commit-hook (2026-05-17) |
| INV-2 | Spec change references corresponding code change; new-field spec adds test | **`.githooks/pre-commit`** (warns when a `N5/specifications/*.md` is staged without an accompanying code/data file under `N5/`). Install via `git config core.hooksPath .githooks`. | Convention + commit-hook (2026-05-17) |
| INV-3 | Code public-API change updates API docs | N/A — project has no traditional API (static SPA + content corpus) | Out of scope |
| INV-4 | Data-file count changes update version.json AND CHANGELOG | **JA-47** (CONTENT-LICENSE.md), **JA-107** (version.json), **JA-112** (AUDIO.md), **JA-115** (README.md), **JA-119** (Spec §7.3 sample) — 5 user-facing surfaces covered | Wired (hard CI) |
| INV-5 | UI string change propagates to all locales | **JA-108** (`locales/*.json` key-set parity) | Wired (hard CI) |
| INV-6 | Prompt change includes a regression test of golden output | **JA-116** (every A-NN / Phase-0 / FP-NN in prompts has ≥1 matching xlsx scenario row; auto-extracted, asserted at CI) | Wired (hard CI; promoted 2026-05-17 from Convention) |
| INV-7 | Cross-file references resolve | JA-15 (audio), JA-17 (vocab_id in grammar), JA-82 (_meta.see_also / consumers), JA-100 (kanji↔vocab form), JA-105 (vocab_preview refs), JA-113 (meta-mirror freshness), **JA-117** (passage_id kanji↔reading + pattern_id reading↔grammar — 363+319 refs all resolve) | Wired (hard CI; promoted 2026-05-17 from Partial) |
| INV-8 | CHANGELOG entry names every dependent updated | **`.githooks/pre-commit` + `commit-msg`** (pre-commit warns when `N5/data/*.json` is staged without `N5/CHANGELOG.md`; commit-msg warns on multi-file commits with short messages). Install via `git config core.hooksPath .githooks`. | Convention + commit-hook (2026-05-17) |
| INV-9 | Closed bug links to fix commit + regression test | **JA-118** (every Fixed row in xlsx User Reported Bugs has a non-empty Fix Commit cell); companion tool `tools/populate_bug_fix_commits_2026_05_17.py` scans git log to back-fill | Wired (hard CI; promoted 2026-05-17 from Partial) |
| INV-10 | Procedure-manual / prompt → script references resolve | **JA-109** (scope: N5 prompts + AUDIT-COVERAGE only; cross-level procedure manual excluded because its refs are abstract Nx targets) | Wired (hard CI) |

**Wired (hard CI):** 6 invariants — INV-4 / INV-5 / INV-6 / INV-7 / INV-9 / INV-10.
**Convention + commit-hook:** 3 — INV-1 / INV-2 / INV-8 (commit-time enforcement via `.githooks/`; install with `git config core.hooksPath .githooks`).
**Out of scope:** 1 — INV-3 (no traditional API).

**Note:** 9 of 10 INV-N classes are now enforced either at CI-time
(hard gate, runs on push + PR via
`.github/workflows/content-integrity.yml`) or at commit-time
(soft gate, runs locally via `.githooks/` if the maintainer has
installed the hook directory). The remaining 1 (INV-3 = API docs)
has no enforcement because the project has no traditional API
surface — static SPA + content corpus only.

When a future audit cycle promotes a convention-only INV to a hard
CI invariant, add the JA-NN row to the appropriate subsection above
AND update the Status column here. The convention→partial→wired
progression is the recommended promotion path.

The structured-report emitter `tools/cross_artifact_sync_report.py`
rolls up this matrix at runtime and exits 0 (CLEAN) when all wired
invariants are green. The text output names each INV-N's status and
links to the JA-NN gates that implement it; the `--json` mode emits
the same data as a machine-readable payload for CI pipelines.

---

## 26. Glossary

| Term | Definition |
|---|---|
| **JLPT** | Japanese Language Proficiency Test (Japan Foundation / JEES). Five levels: N5 (easiest) → N1. |
| **N5** | Lowest JLPT level. Project corpus: 178 grammar patterns, 995 vocab, 106 kanji (no canonical post-2010 list — counts reflect this app's scope decisions). |
| **Moji (文字)** | "Characters." JLPT subsection testing kanji recognition / orthography. |
| **Goi (語彙)** | "Vocabulary." JLPT subsection testing word meaning + paraphrase. |
| **Bunpou (文法)** | "Grammar." JLPT subsection testing particle usage + sentence form. |
| **Dokkai (読解)** | "Reading comprehension." JLPT subsection. |
| **Choukai / Chokai (聴解)** | "Listening comprehension." JLPT subsection. |
| **Furigana** | Small kana written above kanji indicating reading. Rendered as `<ruby><rt>` in HTML; `js/furigana.js` is the renderer. |
| **Kun-yomi** | Native Japanese reading of a kanji (e.g., 山 = やま). Hiragana convention in this corpus. |
| **On-yomi** | Sino-Japanese (imported) reading (e.g., 山 = サン in compounds). Katakana convention enforced by JA-76. |
| **Bika-go (美化語)** | Polite お/ご prefix on common nouns (お茶, お金). NOT honorifics. |
| **Sonkei-go (尊敬語)** | True honorific language (いらっしゃる, おっしゃる). Out of N5 scope. |
| **Te-form (て形)** | Verb conjugation used for sequencing, requests, compound forms. |
| **Tara-form / Tari-form** | Conditional / non-exhaustive listing forms. |
| **SRS** | Spaced Repetition System. Algorithm-driven review scheduling. |
| **SM-2** | SuperMemo-2 algorithm (Wozniak). 4-button grade + ease factor. Was used pre-v1.7.0. |
| **FSRS-4 / FSRS-4.5** | Free Spaced Repetition Scheduler. Replaced SM-2 in v1.7.0; cross-skill unified queue shipped in round-9 IMP-092. |
| **Leitner** | Earlier SRS approach (fixed 1d/3d/7d/14d boxes). Historical only — referenced by v3 .docx but never shipped. |
| **PWA** | Progressive Web App. Installable, offline-capable web app backed by service worker + manifest. |
| **Service worker (SW)** | Browser-side script intercepting fetches; current `CACHE_VERSION` derived from `version.json:version`. |
| **Stale-while-revalidate** | Caching strategy used for shell assets. |
| **Cache-first** | Caching strategy for content + audio (audio is lazy / not pre-cached). |
| **WCAG 2.1 AA** | Web Content Accessibility Guidelines, Level AA. Conformance target. |
| **Pass-N audit** | Content-review protocol; each pass applies a lens (paper-maker, native-teacher, etc.). See §34. |
| **KB / KnowledgeBank** | Former `KnowledgeBank/` directory holding source-of-truth catalog markdown. Merged into `data/` + `docs/N5-syllabus-methodology.md` on 2026-05-14. |
| **CP932** | Microsoft's Shift-JIS superset on Windows JP terminals. Codebase is em-dash-free for cp932 console safety; UTF-8 enforced for emit paths. |
| **gTTS** | Google Text-to-Speech (Python). Used at build time for grammar + reading audio (711 files). |
| **VOICEVOX** | Local TTS engine (free, multi-voice). Used at build time for listening audio (47 files, 4 speakers). |
| **kanjium** | Pitch-accent reference dataset (CC-BY-SA 4.0). Pinned at commit `8a0cdaa` for JA-90. |
| **KanjiVG** | Kanji stroke-order SVG dataset (CC-BY-SA 3.0). 106 N5 glyphs shipped. |
| **JA-NN** | Content-integrity invariant in `tools/check_content_integrity.py`. Full register in §25. |
| **MOB-NNN** | Mobile-UI bug class label (this project's labelling) — see workbook `User Reported Bugs` rows 113-131 for MOB-001..019 filed 2026-05-19. |
| **HIG** | Apple Human Interface Guidelines. Touch-target minimum 44×44 px. |
| **Material** | Material Design (Google). Touch-target minimum 48×48 px. |
| **D-NNN** | Device profile shorthand used in mobile testing: D-320 / D-360 / D-375 / D-390 / D-414 / D-768 / D-360L (landscape). See `O. Mobile UI testing` tab. |
| **Niche-N1** | Strategic positioning: native-Hindi-medium JLPT prep. |
| **Niche-N2** | Strategic positioning: privacy / no-account / offline. |
| **Tofugu-bar / Q33 quality bar** | Pedagogical-depth standard adopted internally — every grammar pattern essay ≥ 500 chars + 6 sub-fields (JA-54 / JA-55). |
| **IMP-NN / ISSUE-NN** | Improvement / issue tracker IDs in `feedback/n5-audit-2026-05-04.xlsx`. |
| **Cross-Artifact Sync Protocol** | Binding rule (CLAUDE.md Rule 5) — when one artifact changes, every other artifact that references it updates in the same commit. INV-1..INV-10 implementation table at §25.10. |

---

## 27. Stakeholders & responsibilities

| Role | Held by | Decision rights | Veto on |
|---|---|---|---|
| **Product owner** | Gaurav Srivastava (continuous) | Final scope + content correctness + release decisions | Anything |
| **Engineering owner** | Gaurav Srivastava (continuous) | Architecture + feasibility | Infeasible-scope items |
| **Content reviewer (JA)** | Suiraku San (commissioned 2026-04-30; partial sign-off ~30 % surface logged) | Validates Japanese accuracy across passes | JA-incorrect content |
| **Content reviewer (HI)** | Currently LLM-curated (no native-Hindi reviewer engaged); recruitment-side IMP-094-class | (no formal seat yet) | Marked LLM-curated until engaged |
| **Accessibility reviewer** | Gaurav Srivastava (acting; axe-core + Lighthouse CI gates) | Validates WCAG 2.1 AA | A11y regressions |
| **Audit-trail keeper** | Gaurav Srivastava (writes `verification.md` + `AUDIT-COVERAGE-YYYY-MM-DD.md`) | Records each audit cycle + outcome | n/a |
| **End learners** | (no formal seat) | Provide feedback via `#/feedback` (mailto-based) | n/a |
| **Hosting** | GitHub Pages | Serves static assets | Out of project control |

**Notes**: four of five named slots are held by the project author on a continuous basis. The Content-reviewer (JA) slot has a named external reviewer with a partial sign-off; full sign-off depends on remaining ~70 % surface coverage. For a future formal release (e.g., MEXT-alignment), the spec recommends recruiting an external A11y reviewer in addition to the automated tooling backing the current slot.

---

## 28. User stories

| Story ID | As a … | I want … | So that … | Maps to |
|---|---|---|---|---|
| US-1 | First-time visitor | a single primary CTA on the home page | I know where to start without reading | §5.1, Home spec |
| US-2 | Returning learner | a "Continue" / "Resume" affordance showing my last lesson | I can resume in one click | §5.1, last-viewed deep-link |
| US-3 | Self-studier | structured grammar lessons with form rules + examples | I can build a model, not memorize | §5.3 |
| US-4 | Late-N5 learner | furigana off by default on N5 kanji | I practice reading, not crutches | Settings.furiganaMode |
| US-5 | Late-N5 learner | a "known kanji" toggle to suppress furigana per kanji | I hide furigana on mastered kanji while keeping it on the rest | §8.2 knownKanji |
| US-6 | Test-taker | mock exams with timed, randomized, balanced sampling | the app feels like a real exam, not memorizable | §5.8 |
| US-7 | Learner | weak-pattern detection over rolling history | one bad day doesn't mislabel my mastery | §5.11 / §5.12 |
| US-8 | Learner | SRS review queue surfacing items at the right interval | I retain what I learn long-term | §5.10 / §5.14 |
| US-9 | Learner | offline access on subway/flight | I can study anywhere | §9 |
| US-10 | Privacy-conscious user | no login, no telemetry, all progress local | my study habits stay private | §13 |
| US-11 | Mobile user | bottom nav and 44 × 44 px tap targets | I can study on a phone with one thumb | §30 NFR-M cluster |
| US-12 | Hindi-medium learner | UI + content explanations in Hindi | I can use the app without strong English | §12, §7.1a |
| US-13 | Power user | keyboard shortcuts (?, /, 1-4, Space, Enter) | I move at the speed of thought | `js/shortcuts.js` |
| US-14 | Cross-device user | export progress to JSON, import on another device | I'm not locked to one machine | §5.12, §8 |
| US-15 | Content author | a content lint that fails commit on out-of-scope vocab/kanji | I can't ship N4 content into N5 by accident | §25.2 (JA-13/JA-16/JA-99) |
| US-16 | Content author | a Pass-N audit protocol with re-audit cadence | content quality stays raised over time | §34 |
| US-17 | Returning user post-update | a non-blocking "Update available" toast | I get fresh content without interruption | §9.3 |
| US-18 | Hindi-L1 learner | L1-transfer notes for common HI→JA particle confusions | I understand WHY my native-instinct answer is wrong, not just THAT it is | grammar.l1_notes.hi |
| US-19 | First-time visitor | a placement / diagnostic so I land in the right module | I don't waste time on what I already know | `#/diagnostic` |
| US-20 | Hindi-L1 learner | every UI string + every grammar/vocab/kanji explanation in Hindi | I never hit an English-only fallback | §12 + §7.1a (MOB-007 flags one currently-untranslated string) |

---

## 29. Success metrics (KPIs)

Measured per release. Without them "done" cannot be defined. **Privacy-respecting**: all KPIs measurable on-device via `localStorage` aggregates or via Lighthouse CI — no remote telemetry.

| Metric | Target | How measured |
|---|---|---|
| Lighthouse Performance (mobile) | ≥ 0.85 | Lighthouse CI per release |
| Lighthouse Performance (desktop) | ≥ 0.95 | Lighthouse CI |
| Lighthouse Accessibility | ≥ 0.95 | Lighthouse CI |
| Lighthouse Best Practices | ≥ 0.85 | Lighthouse CI |
| Lighthouse SEO | ≥ 0.85 (warn-only) | Lighthouse CI |
| Lighthouse PWA | 100 | Lighthouse CI |
| First Contentful Paint | < 1.8 s on slow-4G (150 ms RTT / 1.6 Mbps / 4× CPU) | Lighthouse / WebPageTest |
| Largest Contentful Paint | < 2.5 s on slow-4G | Lighthouse |
| Cumulative Layout Shift | < 0.1 at all viewports | Lighthouse |
| Total Blocking Time | < 200 ms | Lighthouse |
| Critical-path JS + CSS + HTML (gzipped) | < 100 KB | Network panel |
| Total first-load (excl. audio) | < 300 KB | Network panel |
| Audio | lazy-loaded; first lesson page loads without fetching MP3s | Network panel |
| Repeat visit (warm SW cache) | FCP < 100 ms | DevTools |
| Content audit cumulative findings closed | ≥ 99 % (target floor) | `verification.md` + `feedback/n5-audit-2026-05-04.xlsx` |
| JA-accuracy CI invariants passing | 100 % (release-blocker) | `tools/check_content_integrity.py` |
| Mobile horizontal-scroll on every route at D-320/360/375/414/768 | 0 instances | `O. Mobile UI testing` matrix (50/50 PASS at 2026-05-19) |
| Touch-target compliance (HIG 44 ≤) on shipped UI | ≥ 95 % (within MOB-001..016 fix scope) | `tools/run_mobile_ui_tests.py` audit |

**Note**: the lesson-completion + D1/D7/D30 retention metrics from the v3.1 supplement remain *measurable on-device* (`storage.js` exposes the aggregates) but are not currently surfaced in any UI or report. Future work to integrate them would honor the no-telemetry constraint.

---

## 30. Non-functional requirements (consolidated)

This section consolidates the NFR clusters that were scattered across v3 + the v3.1 supplement, integrating today's mobile-UI findings (NFR-M).

### 30.1 Internationalization (NFR-I)

- **NFR-I1** UI shall ship in 2 locales: `en` + `hi` (narrowed from 5 on 2026-05-06 per IMP-096; vi/id/ne/zh migrated silently to `en` via `migrateLocaleSetting()`).
- **NFR-I2** All translatable strings live in `locales/*.json`; no hardcoded UI strings in JS modules.
  - *Current open exception*: MOB-007 — `js/home.js` carried `← All JLPT levels` as a literal; JA-131 invariant prepared; fix pending.
- **NFR-I3** Locale files share an identical key tree (JA-108 enforces); missing keys fall back to English with a console warning.
- **NFR-I4** Japanese content (lessons, examples, questions) is **never** machine-translated. Hindi versions are LLM-curated at the Q33 quality bar (see §12).
- **NFR-I5** Locale preference persists in `localStorage.jlpt-n5-tutor:settings.uiLang` and survives reload.
- **NFR-I6** Locale switch shall not require a page reload; `<html lang>` updates reactively (BUG-8 fix v1.12.46).

### 30.2 PWA (NFR-W)

- **NFR-W1** Installable PWA via `manifest.webmanifest`; Lighthouse PWA score = 100.
- **NFR-W2** Service worker uses **stale-while-revalidate** for shell + **cache-first** for content + lazy for audio (§9.1).
- **NFR-W3** SW broadcasts `SW_UPDATE_AVAILABLE` on fresh shell; UI surfaces non-blocking "Update available — reload?" toast.
- **NFR-W4** App functional offline after first load; offline indicator chip toggles with `navigator.onLine`.
- **NFR-W5** App icons (192 / 512 px) render on mobile home screen; iOS shows icon on splash (no white screen).
- **NFR-W6** Install banner via `beforeinstallprompt`; one-time + dismissible + persists in localStorage.
- **NFR-W7** Cache-bust contract for ES modules: bumping `CACHE_VERSION` in `sw.js` is necessary but not sufficient — `index.html` must also bump `?v=N` on the entry script. JA-68 invariant enforces.
- **NFR-W8** SW `PRECACHE` array must list every static asset reachable on first paint, including the 2 locale files, all JS modules, the 4 woff2 font subsets.
- **NFR-W9** SW scope is `/JLPTSuccess/N5/` only (default-scope-by-script-location). Cache key prefix `jlptsuccess-n5-` namespaced so future N4/N3/N2/N1 SWs cannot collide (§9.5).

### 30.3 Performance (NFR-P)

See KPI table in §29 for measurable budgets. NFR-P statements:

- **NFR-P1** Skeleton loaders so layout doesn't shift on route change (CLS contribution capped).
- **NFR-P2** Self-hosted woff2 fonts (Inter L/R/M + Noto Sans JP 400 N5+N4-subset) ≤ 510 KB total — eliminates flash of system font + third-party CDN dependency.
- **NFR-P3** Audio NOT precached — saves ~22 MB on first install; cached on first fetch per item.
- **NFR-P4** ES modules cached by URL with `?v=…` query string for cache-bust (paired with NFR-W7).
- **NFR-P5** CSS minified at build (`css/main.min.css`, ~34 % smaller). Build via `tools/build_min_css.py`.
- **NFR-P6** No build-step at runtime; no third-party CDN. All bytes ship from the same origin as the HTML.

### 30.4 Accessibility (NFR-A)

- **NFR-A1** Conformance target: WCAG 2.1 Level AA.
- **NFR-A2** axe-core (via Playwright P0 smoke) reports zero `serious` / `critical` violations on Home / Learn / Test / Review / Settings.
- **NFR-A3** All interactive elements have a visible focus ring on keyboard focus (verified: 2 px solid `#14452a` outline + 2 px offset on `.study-order-link` etc.).
- **NFR-A4** Skip-to-content link works from keyboard (`<a class="skip-link" href="#app">`).
- **NFR-A5** `prefers-reduced-motion` disables non-essential animations; user override available in Settings.
- **NFR-A6** Forced-colors / Windows High Contrast mode: all visible text remains visible; no information conveyed by color alone.
- **NFR-A7** Furigana ruby announced correctly by NVDA + Japanese voice (e.g., 学校 reads as がっこう, not がく-こう).
- **NFR-A8** Tap targets ≥ 44 × 44 px (WCAG 2.5.5). *Current open exceptions* tracked at MOB-002..005, MOB-011..016 — see §30.5.
- **NFR-A9** Semantic landmarks: `<header role="banner">`, `<main>`, `<footer>`, `<nav>` with `aria-label`.

### 30.5 Mobile UI (NFR-M)

Breakpoint tier:

| Breakpoint | Scope | Rules at this tier |
|---|---|---|
| `@media (max-width: 768px)` | Phone + tablet portrait | Body type-floor, overflow-x safety belt, smooth scroll, iOS-zoom inputs, tap feedback, h2 cap, auxiliary-control tap-target rules |
| `@media (max-width: 480px)` | Small phones | Pattern-header column-stack, secondary-nav single-row, footer center, primary-nav fixed bottom, body padding-bottom for safe-area |
| `@media (max-width: 380px)` | Galaxy S9+ tier | Bottom-nav font/padding/min-width crunch |

NFRs:

- **NFR-M1 Tap-target floor.** Every interactive element ≥ 44 px on its smallest dimension. Auxiliary controls (icon-btn, breadcrumb back-link, footer-nav links, pattern-nav arrows, known-toggle) carry mobile-only `min-height: 44px` rules. *2026-05-19 status*: MOB-002..005, MOB-011..016 catalogue currently-failing instances; JA-132 invariant prepared as drift-guard.
- **NFR-M2 iOS auto-zoom prevention.** Every text/search/email/number input + textarea + select at ≥ 16 px on `≤768` breakpoint. *Current gap*: MOB-006 — 4 feedback-form inputs at 14.06 px; JA-133 invariant prepared.
- **NFR-M3 Overflow safety belt.** `html, body { overflow-x: hidden; }` at `≤768`. Defensive; verified 0 horizontal-scroll instances across 10 routes × 5 viewports = 50/50 PASS on 2026-05-19.
- **NFR-M4 Body type-floor.** `body { font-size: max(16px, var(--text-base)); }` at `≤768`. Verified: 16 px at D-320.
- **NFR-M5 Visible tap feedback.** Every primary tap target gets `:active { transform: scale(0.97); opacity: 0.85; transition: 80ms }` at `≤768`. Mobile has no `:hover` equivalent.
- **NFR-M6 Smooth scroll.** `html { scroll-behavior: smooth; }` at `≤768`.
- **NFR-M7 Container gutter.** At `≤480`, `main + .app-footer + .app-header` collapse inline padding from desktop's 22.5 px to 16 px. Avoid universal `* { max-width: 100% }` (constrains SVG icons + brand-mark pseudo-elements).
- **NFR-M8 Heading hierarchy on narrow screens.** `h2` clamps at 18 px (not desktop 22 px) at `≤768`. Preserves typographic cascade on 320 px.
- **NFR-M9 Desktop safety.** Every mobile sweep ships with a 1280 px regression check. Desktop default MUST remain byte-identical (body overflow-x: visible, scroll-behavior: auto, body font ~14.06 px, main padding 22.5 px, no `:active` rule, h2 22 px). Mobile rules MUST cut off cleanly at 769 px.
- **NFR-M10 Primary nav completeness on mobile.** All 9 primary nav links must remain visible-and-tappable on D-360. *Current gap*: MOB-001 — `test` + `summary` go to `display: none` on mobile widths with no overflow menu; JA-131 prepared.
- **NFR-M11 Route resolution.** Every internal `href="#/…"` reference must resolve to a route handled by `js/app.js`; silent redirects (#/listening/story → #/listening, #/levels → #/diagnostic) are deprecated. JA-134 + JA-135 invariants address.

**Mobile QA matrix** (per release): test at 320 / 360 / 390 / 480 / 768 + 1280-px desktop safety check:

```
[ ] No horizontal scroll  (document.documentElement.scrollWidth === clientWidth)
[ ] All tap targets ≥ 44 px on smallest dimension
[ ] body { font-size } ≥ 16 px at all mobile widths
[ ] All inputs ≥ 16 px (iOS zoom test)
[ ] Card descriptions show full text or clean line-aligned ellipsis
[ ] Smooth scroll on anchor jumps
[ ] Desktop @ 1280: every NFR-M9 value matches pre-mobile-sweep defaults
[ ] No silent route redirects on internal hrefs
```

### 30.6 Disabled-control feedback (NFR-U)

Any control that can be disabled MUST display the reason in visible UI text — not only in a `title` tooltip. Tooltips don't fire on touch; mobile users get a silent dead control.

| Pattern | Required visible reason | Reference site in this app |
|---|---|---|
| Submit / Finish disabled until all answered | "(N remaining)" in button text + hint paragraph | `js/papers.js`, `js/test.js`, `js/diagnostic.js` |
| Check Answer disabled until any answer | Type-aware hint above button | `js/drill.js` |
| Confirm disabled until typed phrase | Input field IS the visible reason | `js/settings.js` |
| Prev / Next at first / last item | Position context (progress meter) makes it obvious | – |
| Per-choice disabled after submission | Feedback panel below shows result | – |

**Saved-toast pattern** (silent settings): for settings that save but have no immediate visible side-effect (daily-new-card limit, daily-review-cap, default test length, audio-rate, reduce-motion), `js/settings.js` shows a brief on-screen toast on every change: `Saved: <label> = <value>`. CSS class `.settings-saved-toast` — fixed-position bottom-centre, 180 ms fade, 1800 ms auto-dismiss, respects `prefers-reduced-motion`.

**Export-action confirmation**: file-dialog easily missed in browser's downloads bar; `js/settings.js` shows status line under Export: `Exported to <filename> (check your downloads folder).` Auto-clears after 4 s.

**Pre-release checklist** (NFR-U1):

```
[ ] Every <button disabled> rendering site has visible reason text
[ ] Every settings setter without immediate visual effect has a saved-toast
[ ] Every async action with > 200 ms latency has a loading or status indicator
[ ] Every disabled control verified by tap (not hover) on a touch-only browser
```

### 30.7 Security (NFR-S)

See §15 for the active CSP. NFR statements:

- **NFR-S1** CSP `script-src 'self'` deliberately omits `'unsafe-inline'` — any inline-script regression fails fast.
- **NFR-S2** No third-party scripts shipped (cross-checked at every release).
- **NFR-S3** Mailto-based feedback only; no POST endpoints.
- **NFR-S4** No user-content upload surface; XSS exposure is zero-by-architecture.

### 30.8 Privacy (NFR-PR)

- **NFR-PR1** No accounts. No login. No auth.
- **NFR-PR2** No telemetry. No analytics. No third-party scripts.
- **NFR-PR3** No remote API calls during normal use.
- **NFR-PR4** All state in localStorage (browser-local, namespaced `jlpt-n5-tutor:`).
- **NFR-PR5** Service worker only caches same-origin assets.
- **NFR-PR6** Verifiable by opening DevTools → Network: only same-origin requests should ever appear.

---

## 31. Risks register

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Content edit silently introduces unnatural Japanese | Med | High | §25 CI invariants + quarterly Pass-N re-audit (§34) |
| Service worker caches stale shell after a release | Low | Med | Stale-while-revalidate + update toast + JA-68 cache-version sync |
| Browser autoplay policy blocks audio on first play | Med | Low | Only play audio on user gesture; iOS-Safari case documented in §11 |
| Content drifts from KnowledgeBank as JSON edits go in | Low | Med | KB merged into `data/` 2026-05-14 — drift class extinguished |
| Locale file falls behind English (missing keys) | Low | Med | JA-108 enforces key-set parity; periodic key-tree diff in CI |
| GitHub Pages deprecates `/docs` deployment path | Low | Med | Switch to `gh-pages` branch; both supported |
| Vendor lock-in on gTTS / VOICEVOX | Low | Low | `tools/build_audio_voicevox.py` auto-falls-back to edge-tts then gtts |
| Author becomes unavailable; no continuity plan | Low | High | Spec + verification.md + procedure-manual-build-next-jlpt-level.md cover handoff; KB human-readable |
| Misclassified Group-1 ru-verbs (入る etc.) regress | Med | High | X-6.6 ru-verb exception flags enforced in CI |
| Mobile-UI batch (MOB-001..019) lingers Open | Med | Med | JA-131..135 invariants prepared as regression guards; maintainer triage gated |
| Native-Hindi reviewer not engaged → quality claim weakens | Med | Med | Currently LLM-curated at Q33 bar; documented honestly in §27 |
| Native-Japanese-teacher review only partial (~30 %) | Med | Med | Partial sign-off logged; full pass due 2026-07-30 per §27 |
| iOS Safari auto-zoom on feedback form | High (until fixed) | Low | MOB-006 filed; JA-133 prepared |
| Touch-target violation density on `#/authentic` | High (currently) | Low | MOB-011 filed; design-density tradeoff documented |
| Silent route redirect for `#/listening/story`, `#/levels` | High (currently) | Low | MOB-008/009 filed; JA-134/135 prepared |
| Pass-N audit cadence slipped (calendar drift) | Low | Med | Quarterly cadence + GitHub Actions monthly reminder workflow |
| LLM-curated Hindi flagged as native-reviewed | Low | High | R-0 provenance honesty rule + JA-35 closed-enum review_status |
| Forced-colors / High Contrast Mode regression | Low | Med | NFR-A6; checked manually each release |

---

## 32. Maintenance & release model

### 32.1 Maintenance

- **Content errors** (typos, wrong readings, ungrammatical sentences): fix in `data/*.json` directly (no longer a separate KB markdown step). Severity → CRITICAL block release, HIGH next release, MEDIUM/LOW batch into next quarterly Pass-N. Log in `feedback/n5-audit-2026-05-04.xlsx` + the relevant `AUDIT-COVERAGE-*.md`.
- **Engine bugs**: file in `User Reported Bugs` tab of the workbook (`specifications/test-scenarios-by-specialist-perspective.xlsx`); reproduce via Playwright P0 smoke + mobile Selenium runner; fix; add regression test + new JA-NN invariant where deterministic.
- **A11y regressions**: treat as P1 — block release.

### 32.2 Cadences

- Daily: nothing required; static site.
- Per release: P0 smoke (`tests/p0-smoke.spec.js`) + Mobile UI Selenium (`tools/run_mobile_ui_tests.py`) + content-integrity (152 invariants at this checkpoint).
- Quarterly: Pass-N re-audit per §34.
- Annually: full spec review + amendment (this document).

### 32.3 Release process

- Each release advances `version.json:version` (e.g., `v1.15.5` → `v1.15.6`).
- `CACHE_VERSION` in `sw.js` mirrors automatically.
- `CHANGELOG.md` is appended; static mirror at `changelog/index.html` regenerated via `tools/build_static_mirrors.py --stages meta` (JA-113 catches drift).
- Tag commit as `v<X.Y.Z>`.
- TS-5 release-blocker list (any one failing blocks): (a) `tools/check_content_integrity.py` reports < 100 % PASS; (b) Playwright P0 smoke fails; (c) Lighthouse below budget; (d) any OPEN CRITICAL audit finding; (e) Mobile-UI Selenium reports any P1 regression.

### 32.4 Procedure manual

The cross-level build manual at `JLPT Common/procedure-manual-build-next-jlpt-level.md` (separate git submodule `gauravaccentureproducts/JLPT-Common`) captures the *how-to* for building Nx levels from this N5 source. Per CLAUDE.md Rule 4, any learning-producing change in N5 propagates to this manual + the audit prompts + this spec in the same commit.

---

## 33. Copy voice contract

Set 2026-04-30 (supersedes UX brief-2 §1.1 / §15 tagline language) and reaffirmed in every release since.

1. **No outcome promises.** Don't say what the user will achieve.
2. **No time-to-result claims.** No "in 15 minutes a day", "quickly", "easily".
3. **No second-person imperatives at the brand level.** `Start your first lesson` → `Start a lesson`.
4. **No celebration glyphs.** No ✓-prefixed badges, no ★ Graduated.
5. **No defensive claims.** Describe properties plainly. "No login required" → just don't have login.
6. **No gamification at the surface.** "Keep your streak alive" → just show the count.
7. **Prefer noun phrases to headlines.** "Today's review queue" → "Reviews due today".
8. **One register across microcopy.** No mixing onboarding-funnel with neutral.

**Trust-strip handling** (v1.6.1 → v1.12.47): the body-page trust-strip ("✓ Works offline · ✓ No login required · ✓ Your progress stays on this device") was removed in v1.6.1. As of v1.12.47, the *promoted* trust band lives in the home + level-picker + footer + install banner — re-introduced because the niche-N2 message had been buried. The phrasing has been de-checkmarked and made declarative: `No login · No tracking · Works offline · Open source · 100 % on-device · Free · No ads · No paywall`.

**Brief-2 explicit overrides** (still active):

- Brief-2 §1.1.5 (trust strip): superseded by promoted-band design (v1.12.47).
- Brief-2 §15 (outcome-promise tagline): removed entirely. Home opens directly into syllabus grid.
- Brief-2 §11.4–11.6 (reduce noise / sentence case / drop celebratory checkmarks): still active.

**Active reference** for tagline / CTA / microcopy: (a) live `js/home.js` / `js/summary.js` / `js/drill.js` / `js/counters.js` / `index.html`, (b) the rewrite table in `feedback/n5-audit-2026-05-04.xlsx` "Copy audit", (c) §35 (motion contract) for the visual-voice counterpart.

---

## 34. Content audit protocol (Pass-N)

Normative protocol that produced the 462 → 789 cumulative findings closed in `verification.md`, refined into the 9-round audit cycle in `feedback/n5-audit-2026-05-04.xlsx` (closed at 219 Done / 3 Avoid / 0 Open / 0 OQ at v1.12.50) and the per-cycle docs at `docs/AUDIT-COVERAGE-YYYY-MM-DD.md`.

### 34.1 Audit lenses (catalogued historical + ongoing)

| Pass | Lens | Findings | Closed |
|---|---|---|---|
| 1-4 | Initial KB content checks | 24 | 24 |
| 5 | First batch corrections | 11 | 11 |
| 6 | JLPT paper-maker perspective | 39 | 39 |
| 8 | Native Japanese teacher | 52 | 52 |
| 9 | External 日本語教師 | 38 | 38 |
| 10 | Audio + auto-furigana correctness (TTS) | 309 | 309 |
| 11–14 | (baseline) | 91 | 91 |
| 15 | Multi-correct sweep + grey-zone audit | 28 | 28 |
| 15a | Free heuristic audit (`tools/heuristic_audit.py`) | 60 | 60 |
| 16 | Pre-native-review dossier prep | 14 | 14 |
| 17 | KnowledgeBank PoS-tag injection + JA-31 | 37 | 37 |
| 18-19 | Late-tier reclassification + 14 patterns promoted | 14 | 14 |
| 20-22 | Procedure manual review + appendices + D-rules | 61 | 57 + 4 deferred |
| 23 | Provenance lock-in (CONTENT-LICENSE + JA-30) | – | – |
| 24 | Seasoned-teacher review (T-1..T-9) | 12 | 12 |
| 25-26 | Native-dossier audit + closure verify | 14 | 14 |
| 27 | Data-files audit (grammar/vocab/kanji JSON) | 10 | 8 + 2 deferred |
| Rounds 1-9 (2026-05-04 → 2026-05-08) | Mixed-lens, tracker-driven | 219 actionable | 219 Done / 3 Avoid / 0 Open |
| 2026-05-14 → 2026-05-15 mega-audit | Programmatic + sample-based native-teacher | ~1900 fixes | within named categories |
| 2026-05-15 | Pitch-accent reconciliation (kanjium) | 944 high-conf MATCH + 22 fixes + 43 low/unverified | within reference scope |
| 2026-05-17 → 2026-05-18 bug batches | LLM crawler / paper / dokkai / register | BUG-094..109 + REG-001 | All closed at commit |
| 2026-05-19 mobile-UI test run | Selenium + secondary probe | MOB-001..019 + DOKKAI-004 | All Open (per maintainer instruction) |

### 34.2 Pass-N triggers

- **Calendar**: every quarter (next: 2026-08-15).
- **Event**: any batch of 10+ KB/data edits between releases.
- **Event**: any new audit lens proposed.
- **Event**: any externally-supplied audit brief.

### 34.3 Pass-N execution

1. Pick the lens; document it in the new pass entry header in `verification.md` + the relevant workbook tab.
2. Read the affected corpus subset.
3. Log findings with severity (CRITICAL / HIGH / MEDIUM / LOW); register in workbook.
4. Fix in priority order; record per-finding closure.
5. Re-run §25 invariants; all must pass.
6. Mark pass closed; update cumulative tally; commit + push following the file-based commit pattern.
7. Per CLAUDE.md Rule 4 — if the pass produced a learning, propagate to procedure manual + audit prompts + this spec in the same commit batch.

### 34.4 Severity definitions

| Severity | Definition | Release impact |
|---|---|---|
| **CRITICAL** | Factual error, internal contradiction, or content that would teach **wrong Japanese** | Blocks release; hotfix |
| **HIGH / Major** | Unidiomatic phrasing, register clash, policy violation, or UX-functional defect | Fix in next release |
| **MEDIUM / Minor** | Pedagogical clarity improvement, minor UX polish | Batch into next quarterly pass |
| **LOW / P5** | Polish item | Batch into next quarterly pass |

### 34.5 The bar (non-negotiable)

- Zero CRITICAL findings open at release.
- 100 % of CI invariants passing.
- Pass-N protocol followed for every cadence event.
- Writing-discipline applied to every audit-coverage doc (`docs/AUDIT-COVERAGE-*.md`) — see §38 + `prompts/Japanese language Accuracy check.txt`.

---

## 35. Motion & microinteractions contract

Set 2026-05-01 (v1.8.0 Zen Modern design overhaul) and locked by CI design-system checker `tools/check_design_system.py` (8 D-rules; relevant ones below).

### 35.1 Forbidden motion

The UI design brief originally called for:

- Card hover lift (`translateY -2px`)
- Button press scale (`transform: scale(0.98)` on `:active`)
- Shake animation on incorrect answer
- Pulse / glow on correct answer

The Zen Modern overhaul **forbids** these. Specifically:

- **D-3** No `box-shadow:` declarations (other than `none`)
- **D-4** No `transform:` inside `:hover` blocks (`:active` is allowed only for NFR-M5 tap feedback at `≤768`)
- **D-8** No decorative `text-shadow`

Forbidden patterns enumerated: bouncy springs, confetti, **card lift / shadow on hover**, wiggle, shake, pulse, glow, sliding panes, fading in entire pages from blank.

### 35.2 Allowed affordances

Hover affordances render via:

1. Background lightening (surface → surface-alt).
2. Border-color strengthening (line → line-strong).
3. Underline on `.card-action` text only.

Correctness feedback in drill / test surfaces uses **color + class change only** — `.choice-button.correct-choice` gets `color: var(--color-correct)` + tinted background; `.choice-button.wrong-choice` gets the incorrect tint + strikethrough. No motion, no shake.

### 35.3 Mobile exception (NFR-M5)

A brief `:active { transform: scale(0.97); opacity: 0.85; transition: 80ms }` on primary tap targets at `≤768` is the **only** allowed motion-rule that involves transform. Mobile has no `:hover` equivalent; without this, tap-down feels unresponsive on slow networks.

### 35.4 Locked by

- CI design-system check (`tools/check_design_system.py`) — D-3 / D-4 / D-8 rules.
- Any future re-introduction of the UI design brief's microinteractions would fail D-3 / D-4 / D-8 in CI; this guards Zen Modern against drift to SaaS-default microinteractions.

---

## 36. Design system (Zen Modern) — companion document pointer

The full design system (color tokens light + dark mode parity, typography scale + weights + letter-spacing + line-height + headline templates, spacing scale + container widths + page rhythm + border radius + hairlines, components — header / buttons / cards / numbered indicator / pills + badges / progress bars / form controls / tables, page-by-page treatments, furigana rendering, iconography, motion (per §35 above), imagery + ornament, responsive behavior + breakpoints + tap targets, accessibility implementation checklist) was merged into the v3.1 supplement as §G on 2026-05-04. It is **preserved unchanged** at the bottom of `JLPT-N5-Functional-Spec-v3.1-supplement.md` (now a redirect stub, but the §G design-system content remains there for traceability).

**Active rules from the design system that survive into this canonical spec**:

- `PRIMARY_DARK = #14452a`, `LINK_GREEN = LINK_BLUE = #1a5c38` (token reference).
- Font stack: Inter (Latin) + Noto Sans JP (CJK), 4 woff2 files self-hosted (~503 KB).
- Font weights: 300 / 400 / 500 only (D-2).
- Border radius: 2 / 4 / 6 / 8 / 999 (D-6).
- Text transform: `uppercase | none | capitalize` (D-7).
- No pictograph emojis in UI strings (D-1).
- Brand accent via tokens only (D-5).

A future revision may extract §G to a standalone `specifications/jlpt-n5-design-system-zen-modern.md`; until then, treat the supplement file as the design-token canonical source.

---

## 37. Acceptance criteria for spec changes

When this spec is updated (drift correction, new section addition, version bump), the change is acceptable when:

1. The "Last updated" line in the header is bumped.
2. Every claim added has a bounded scope (per writing-discipline note in the header).
3. Numeric counts agree with the live `data/version.json` (JA-119 enforces for §7.3 sample block).
4. CI invariant count in §25 matches live `tools/check_content_integrity.py`.
5. Any new section is cross-referenced from the table of contents implied by `^## ` headings.
6. Historical references (v3 .docx, supplement, .bak files) are preserved as record, not deleted.
7. If the change is a learning-producing audit cycle: procedure manual + prompts + this spec are updated in the same commit batch (CLAUDE.md Rule 4).
8. The supplement file is not re-expanded; new content goes here.

---

## 38. Historical record + supersession map

### 38.1 Document supersession

| Predecessor | Date | Status | Where the content lives now |
|---|---|---|---|
| `JLPT N5 Grammar Tutor – Functional Spec.docx` (v3) | 2026-04-30 informal amendment | Historical | Content merged into this doc + supplement §G design system. Do not edit. |
| `JLPT N5 — Consolidated Spec.docx` (v3 consolidated) | 2026-04-30 | Historical | Same. |
| `JLPT-N5-Functional-Spec-v3.1-supplement.md` (gap-fill addendum) | 2026-04-30 → 2026-05-04 | Reduced to redirect stub on 2026-05-19; §G Zen Modern design system preserved at the bottom of that file | Sections B (governance), C (errata), D (audit protocol), F (mobile UI / data audit) merged into §§26–35 here. §G design system kept in-place at supplement tail. |
| `not-required/` v1 + v2 archives | pre-2026 | Archived | Not in active use; reference via git history. |

### 38.2 Map of supplement subsections → this document

| Old section in supplement | New home |
|---|---|
| B.1 Document control | §1 (this doc) |
| B.2 Glossary | §26 |
| B.3 Stakeholders | §27 |
| B.4 User stories | §28 |
| B.5 Success metrics (KPIs) | §29 |
| B.6.1 NFR-I Internationalization | §30.1 |
| B.6.2 NFR-W PWA | §30.2 |
| B.6.3 NFR-P Performance | §29 + §30.3 |
| B.6.4 NFR-A Accessibility | §30.4 |
| B.7 Test strategy | §19 + §25 + §32.2 |
| B.8 Risks register | §31 |
| B.9 Open questions / decisions log | Historical (workbook + verification.md); see §22 + §23 for current decision state |
| B.10 Maintenance and support model | §32 |
| B.11 Release process | §32.3 |
| B.12 Copy voice contract | §33 |
| B.13 External-blocked items reframed | Historical (verification.md + IMP-094 / IMP-096 / OQ-2 closures) |
| B.14 Pass 15a heuristic audit | §34.1 |
| B.15 Microinteractions vs Zen Modern | §35 |
| C.1 Scope movement (Audio etc.) | §22 |
| C.2 Global navigation rewrite | §4.2 + §5.1 |
| C.3 §5 sub-sections | §5.1 – §5.16 |
| C.4 Drill / SRS Leitner → SM-2 → FSRS-4.5 | §5.9 + §5.10 + §22 |
| C.5 Data model additions | §7 + §8 |
| C.6 Repository structure | §3.1 |
| C.7 NFR replacement | §30 |
| C.8 Future enhancements rewrite | §23 |
| D Pass-N audit protocol | §34 |
| E Acceptance criteria for v4 | §37 |
| F.1 Mobile UI NFR-M1..M9 | §30.5 (with NFR-M10/M11 added 2026-05-19) |
| F.2 Disabled-button feedback NFR-U | §30.6 |
| F.3 JA-25..JA-33 invariants | §25 (extended through JA-139+) |
| F.4 Anti-patterns AP-7..AP-10 | Procedure manual (Appendix F) + workbook tabs |
| F.5 Corpus state snapshot | §7.1 |
| F.6 New routes | §4.2 |
| F.7 SW version + PWA contract | §9 + §30.2 |
| F.8 Pass-N audit log | §34.1 |
| F.9 Procedure manual + companion docs | §32.4 + procedure manual itself |
| F.10 Open / deferred items snapshot | Historical (workbook + verification.md) |
| G Design System Zen Modern | §35 (motion) + §36 (pointer); full tokens preserved at supplement tail |

### 38.3 Writing-discipline lineage

The writing-discipline rewrite (2026-05-15, commit `e1f6dd5`) introduced bounded-phrasing rules into every audit document + the audit prompts + procedure manual + Rule 4 of CLAUDE.md. This spec adheres: every "every / all / complete / final / saturated / closed enum" reference is qualified by what was measured. See `prompts/Japanese language Accuracy check.txt` → WRITING DISCIPLINE FOR AUDIT DOCS section for the rewrite table, and `prompts/N5Improvement.txt` → Phase-0 Audit-doc writing-discipline scan for the regression check.

### 38.4 Recent activity (2026-05-15 → 2026-05-19)

| Date | Event | Outcome |
|---|---|---|
| 2026-05-15 | Audit-coverage doc family installed | `docs/AUDIT-COVERAGE-2026-05-15.md` + 92 CI invariants green |
| 2026-05-15 | Writing-discipline rewrite | 5 audit docs + 2 prompts + procedure manual + Rule 4 — all softened |
| 2026-05-15 | Pitch-accent reconciliation (kanjium) | 944 high-conf MATCH; 22 fixes; 43 low/unverified flagged |
| 2026-05-17 | BUG-094..109 close-out batch | LLM-001..005 + REG-001 + PAPER-001..004 + DOKKAI-001..003 fixed; JA-123..130 wired |
| 2026-05-18 | More bug close-outs + invariant additions | 119 invariants green |
| 2026-05-19 | Test scenarios xlsx built (240 scenarios) | 14 specialist tabs A-N |
| 2026-05-19 | Mobile UI testing tab added | 134 new scenarios (39 cross-cutting + 95 per-screen) |
| 2026-05-19 | Coverage analysis report generated | `docs/TEST-COVERAGE-ANALYSIS-2026-05-19.md` — 606 total scenarios |
| 2026-05-19 | Mobile UI Selenium run | 91 PASS / 7 raw FAIL; 19 MOB bugs filed (rows 113-131) per maintainer instruction (no fix) |
| 2026-05-19 | This merge (spec + supplement) | One canonical spec; 139 CI invariants green |

---

*End of specification.*

*Merged 2026-05-19 from `JLPT-N5-Current-Implementation-Spec.md` (canonical) + `JLPT-N5-Functional-Spec-v3.1-supplement.md` (gap-fill addendum). Supplement reduced to redirect stub in the same commit batch. Design system (Zen Modern §G) preserved at the supplement tail pending future extraction to a standalone file.*
