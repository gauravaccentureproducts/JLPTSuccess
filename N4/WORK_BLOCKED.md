# N4 IS WORK-BLOCKED

**Set:** 2026-05-04 by user directive.

Do not edit any file in this directory tree until the user explicitly unblocks it.

This includes:
- Data files (`data/*.json`)
- Knowledge bank (`KnowledgeBank/*.md`)
- App code (`js/*`, `css/*`, `index.html`)
- Tools (`tools/*.py`)
- Tests, configs, audio, SVGs — everything.

The N4 deploy at `/JLPTSuccess/N4/` continues to serve so existing URLs don't 404, but no further development happens here until reactivated.

**To unblock:** the user must issue an explicit instruction like "unblock N4" or "resume N4 work." See [`/.claude/CLAUDE.md`](../.claude/CLAUDE.md) for the full governance rule.
